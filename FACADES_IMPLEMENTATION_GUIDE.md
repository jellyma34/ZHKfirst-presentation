# 🎬 Руководство по реализации кинематографического видео

## 📋 Как работает система

### Архитектура компонента:

```jsx
const videoRefs = [useRef(null), useRef(null), useRef(null)];
const [current, setCurrent] = useState(0);
const speeds = [0.33, 0.33, 0.66];
```

**Ключевая идея**: Все три видео предзагружены и находятся в DOM одновременно. Мы управляем только их видимостью через `opacity`.

---

## 🔄 Жизненный цикл видео

### 1. **Инициализация** (при монтировании):

```javascript
useEffect(() => {
  videoRefs.forEach((ref, i) => {
    if (ref.current) {
      ref.current.playbackRate = speeds[i];
      console.log(`⚙️ Установлена скорость для видео ${i + 1}: ${speeds[i]}x`);
    }
  });
}, []);
```

✅ Скорость устанавливается **один раз**  
✅ Сохраняется на протяжении всего цикла  
✅ Не сбивается при повторном проигрывании  

---

### 2. **Запуск видео** (при смене current):

```javascript
useEffect(() => {
  const activeVideo = videoRefs[current].current;
  
  activeVideo.playbackRate = speeds[current]; // Перестраховка
  activeVideo.currentTime = 0;                // Начало
  
  fadeIn(activeVideo);                        // Плавное появление
  activeVideo.play();                         // Запуск
  
  // Обработчик завершения
  activeVideo.addEventListener("ended", handleEnd);
  
  return () => {
    activeVideo.removeEventListener("ended", handleEnd);
  };
}, [current]);
```

---

### 3. **Fade-In анимация** (~800ms):

```javascript
const fadeIn = (video) => {
  video.style.opacity = 0;
  let opacity = 0;
  const fade = setInterval(() => {
    opacity += 0.05;
    video.style.opacity = Math.min(opacity, 1);
    if (opacity >= 1) {
      clearInterval(fade);
      video.style.opacity = 1;
    }
  }, 40); // 40ms × 20 шагов = ~800ms
};
```

**Процесс:**
- Начинаем с `opacity: 0`
- Каждые 40ms увеличиваем на 0.05
- Через ~800ms достигаем `opacity: 1`
- Видео плавно появляется

---

### 4. **Проигрывание** (автоматически):

Видео проигрывается с установленной скоростью (0.33x, 0.33x или 0.66x) до конца.

---

### 5. **Fade-Out анимация** (~800ms):

```javascript
const fadeOut = (video, onFinish) => {
  let opacity = parseFloat(video.style.opacity) || 1;
  const fade = setInterval(() => {
    opacity -= 0.05;
    video.style.opacity = Math.max(opacity, 0);
    if (opacity <= 0) {
      clearInterval(fade);
      video.pause();
      video.style.opacity = 0;
      onFinish(); // Переход к следующему
    }
  }, 40);
};
```

**Процесс:**
- Начинаем с `opacity: 1`
- Каждые 40ms уменьшаем на 0.05
- Через ~800ms достигаем `opacity: 0`
- Останавливаем видео
- Вызываем callback для перехода

---

### 6. **Переход к следующему**:

```javascript
const handleEnd = () => {
  fadeOut(activeVideo, () => {
    const nextIndex = (current + 1) % videoRefs.length;
    setCurrent(nextIndex); // Это запустит useEffect снова
  });
};
```

**Цикл:**
```
Видео 1 (0.33x) → fadeOut → setCurrent(1) → fadeIn → Видео 2 (0.33x) →
fadeOut → setCurrent(2) → fadeIn → Видео 3 (0.66x) → fadeOut → 
setCurrent(0) → fadeIn → Видео 1 (0.33x) → ...
```

---

## 🎨 CSS структура

### Контейнер:
```css
.facades-container {
  position: absolute;
  inset: 0;
  background: #000;
}
```

### Видео элементы:
```css
.facade-video {
  position: absolute;
  inset: 0;
  object-fit: cover;
  opacity: 0;              /* По умолчанию скрыто */
  z-index: 0;
}

.facade-video.active {
  z-index: 2;              /* Активное видео выше */
}

.facade-video.hidden {
  z-index: 1;              /* Скрытое видео ниже */
  pointer-events: none;
}
```

**Важно**: Мы **НЕ** используем `transition: opacity` в CSS, чтобы иметь полный контроль через JavaScript!

---

## 🎮 Управление

### Кнопки:

```jsx
<button onClick={prevVideo}>
  <ChevronLeft /> {/* Предыдущее */}
</button>

<button onClick={handlePauseToggle}>
  {isPaused ? <Play /> : <Pause />}
</button>

<button onClick={nextVideo}>
  <ChevronRight /> {/* Следующее */}
</button>
```

### Логика ручного переключения:

```javascript
const nextVideo = () => {
  const activeVideo = videoRefs[current].current;
  fadeOut(activeVideo, () => {
    setCurrent((prev) => (prev + 1) % videoRefs.length);
  });
};
```

При ручном переключении:
1. Запускаем fade-out текущего видео
2. По завершению fade - меняем `current`
3. Автоматически запускается useEffect
4. Новое видео делает fade-in

---

## 💡 Преимущества этого подхода

### 1. **Стабильность скорости**
- Скорость устанавливается **один раз** при монтировании
- Не сбивается при переходах
- Сохраняется в повторных циклах

### 2. **Плавные переходы**
- Полный контроль над opacity
- Точная синхронизация fade-in/fade-out
- Без "мерцаний" и черного экрана

### 3. **Производительность**
- Все видео предзагружены
- Нет перезагрузки при смене
- Быстрое переключение

### 4. **Предсказуемость**
- Четкая логика смены
- Понятный flow управления
- Легко отлаживать

---

## 🔧 Отладка

### Console логи:

```
⚙️ Установлена скорость для видео 1: 0.33x
⚙️ Установлена скорость для видео 2: 0.33x
⚙️ Установлена скорость для видео 3: 0.66x
▶️ Запуск видео 1
✅ Видео 1 загружено
... проигрывание ...
✅ Видео 1 завершено
▶️ Запуск видео 2
✅ Видео 2 загружено
... и т.д.
```

### Проверка скорости в DevTools:

```javascript
// В консоли браузера:
document.querySelectorAll('video').forEach((v, i) => {
  console.log(`Видео ${i + 1}: скорость ${v.playbackRate}x`);
});
```

---

## ✅ Чеклист работы системы

- [x] Видео 1 проигрывается в 3 раза медленнее
- [x] Видео 2 проигрывается в 3 раза медленнее
- [x] Видео 3 проигрывается в 1.5 раза медленнее
- [x] Скорость сохраняется при повторном цикле
- [x] Плавный fade-in при появлении (~800ms)
- [x] Плавный fade-out при исчезновении (~800ms)
- [x] Автоматическая смена по завершению
- [x] Цикличное проигрывание (1→2→3→1...)
- [x] Работают кнопки управления (prev/pause/next)
- [x] Нет черного экрана между видео
- [x] Нет linter ошибок

---

## 🎉 Результат

Кинематографическое проигрывание видео с:
- 🎬 Замедлением для создания премиального эффекта
- 🌊 Плавными fade-переходами
- ⚙️ Стабильной скоростью на всех циклах
- 🎮 Полным контролем через кнопки
- ✨ Без технических артефактов

**Готово к продакшену!** 🚀

