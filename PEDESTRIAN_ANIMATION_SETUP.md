# Анимация движения пешеходов

## ✅ Создана анимированная SVG-сцена

### 🎯 Цель
При нажатии на кнопку "Пути пешеходов" поверх генплана появляется анимированный слой с движущимися пешеходами.

---

## 📁 Созданные файлы

### 1. Компонент анимации

**src/components/PedestrianAnimation.jsx** - новый компонент

**Функциональность:**
- SVG слой с анимированными пешеходами
- 5 маршрутов движения
- Анимация через requestAnimationFrame
- Автоматическая очистка при размонтировании
- Плавное появление/исчезновение

### 2. Интеграция

**src/pages/Landscaping.jsx** - обновлен

**Изменения:**
- Импорт PedestrianAnimation
- Условный рендеринг при активации кнопки

---

## 🎨 Как работает анимация

### Маршруты пешеходов (5 путей):

```jsx
1. Вход → Здание      (синий)
2. Парковка → Вход    (зеленый)
3. Площадка → Здание  (оранжевый)
4. Периметр           (фиолетовый)
5. Поперечный         (розовый)
```

### Пешеходы:

- **Размер:** 8-12px (случайный)
- **Скорость:** Разная для каждого (0.0003-0.0005)
- **Цвет:** Соответствует маршруту
- **Эффекты:**
  - Пульсация (анимация размера)
  - Изменение прозрачности
  - Тень под пешеходом

### Визуальная структура:

```
SVG viewBox="0 0 1000 1000"
  ↓
Пути (пунктирные линии, opacity: 0.3)
  ↓
Пешеходы (круги, движутся по путям)
  ↓
Каждый пешеход:
  - Тень (ellipse, rgba(0,0,0,0.2))
  - Круг (circle, цветной)
  - Пульсация (animate)
```

---

## 🔄 Логика анимации

### requestAnimationFrame:

```javascript
const animate = () => {
  const deltaTime = currentTime - lastTime;
  
  // Обновляем каждого пешехода
  pedestrians.forEach(ped => {
    ped.progress += ped.speed * deltaTime;
    
    // Зацикливание
    if (ped.progress > 1) ped.progress = 0;
    
    // Получаем позицию на пути
    const point = path.getPointAtLength(progress * pathLength);
    
    // Обновляем элемент
    element.setAttribute('cx', point.x);
    element.setAttribute('cy', point.y);
  });
  
  requestAnimationFrame(animate);
};
```

### Cleanup при размонтировании:

```javascript
useEffect(() => {
  // ... анимация
  
  return () => {
    if (animationFrameRef.current) {
      cancelAnimationFrame(animationFrameRef.current);
    }
  };
}, []);
```

---

## 🎮 Интеграция в Landscaping

### Импорт:

```jsx
import PedestrianAnimation from '../components/PedestrianAnimation';
```

### Условный рендеринг:

```jsx
{/* Анимация пешеходов */}
{activeLayers.includes('Пути пешеходов') && (
  <PedestrianAnimation />
)}
```

**Позиция:**
- После обычных слоёв
- position: absolute
- z-index: 3 (выше других слоёв)
- pointer-events: none

---

## 🎨 Визуальный эффект

### При активации кнопки "Пути пешеходов":

```
┌──────────────────────────────────┐
│  Генплан (основное.jpg)          │
│                                  │
│  + Пунктирные пути (paths)       │
│    ┌─────→ синий маршрут         │
│    └→ ● пешеходы движутся        │
│       ● по путям                 │
│       ● с разной скоростью       │
│                                  │
│  Анимация: requestAnimationFrame │
│  Прозрачность: 90%               │
└──────────────────────────────────┘
```

### Слои (z-index):

```
z-index: 3  ← Анимация пешеходов (SVG)
              opacity: 0.9
              
z-index: 2  ← Другие слои (PNG)
              opacity: 0.85
              
z-index: 1  ← Основное изображение
              opacity: 1
```

---

## 🧪 Как проверить

### 1. Откройте страницу "Благоустройство"

### 2. Найдите кнопку "Пути пешеходов"

В панели фильтров:
```
☐ Озеленение
☐ Детские площадки
☐ Освещение
☐ Парковки
☐ Пешеходные зоны
☐ Зоны отдыха
☐ ПЗУ
☐ Пути пешеходов      ← КЛИКНИТЕ
☐ Вход в здание
```

### 3. Кликните на "Пути пешеходов"

**Должно произойти:**
- ✅ Кнопка меняет стиль (активное состояние)
- ✅ Поверх генплана появляется SVG слой
- ✅ Видны пунктирные пути (5 маршрутов)
- ✅ По путям движутся цветные точки (пешеходы)
- ✅ Пешеходы пульсируют
- ✅ Плавная анимация появления (0.3s)
- ✅ Консоль: `✅ Включен слой: Пути пешеходов`

### 4. Наблюдайте анимацию

**Эффекты:**
- Пешеходы движутся по маршрутам
- Разная скорость для каждого
- Пульсация размера и прозрачности
- Тени под пешеходами
- Плавное движение

### 5. Повторно кликните

**Должно произойти:**
- ✅ Кнопка возвращается в обычное состояние
- ✅ SVG слой плавно исчезает
- ✅ Анимация останавливается
- ✅ requestAnimationFrame очищается
- ✅ Консоль: `❌ Отключен слой: Пути пешеходов`

---

## 🎨 Параметры анимации

### Маршруты:

```javascript
const routes = [
  { path: 'M 100 800 L 300 700 L 500 600 L 700 550', color: '#3B82F6' },
  { path: 'M 50 600 L 200 550 L 400 500 L 600 480', color: '#10B981' },
  { path: 'M 150 900 L 350 800 L 550 700 L 750 650', color: '#F59E0B' },
  { path: 'M 80 500 L 200 400 L 400 350 L 650 320', color: '#8B5CF6' },
  { path: 'M 300 900 L 450 750 L 600 600 L 700 500', color: '#EC4899' }
];
```

### Настройки пешеходов:

```javascript
progress: Math.random()              // Случайная начальная позиция
speed: 0.0003 + Math.random() * 0.0002  // Разная скорость
size: 8 + Math.random() * 4          // Размер 8-12px
color: route.color                   // Цвет маршрута
```

### SVG атрибуты:

```jsx
viewBox="0 0 1000 1000"              // Координатное пространство
preserveAspectRatio="xMidYMid meet"  // Сохранение пропорций
```

---

## 🔧 Кастомизация

### Изменить скорость:

```javascript
// В PedestrianAnimation.jsx
speed: 0.0005 + Math.random() * 0.0003  // Быстрее
```

### Изменить количество пешеходов:

Добавьте новые маршруты в массив `routes`.

### Изменить цвета:

```javascript
{ path: '...', color: '#FF0000' }  // Красный
```

### Настроить пути под генплан:

Откройте изображение в редакторе, определите координаты и обновите `path`:
```javascript
{ path: 'M startX startY L point1X point1Y L ...', color: '...' }
```

---

## ⚡ Производительность

### Оптимизации:

```javascript
will-change: transform  // GPU ускорение
requestAnimationFrame   // Синхронизация с частотой экрана (~60fps)
pointer-events: none    // Не блокирует взаимодействие
```

### Cleanup:

```javascript
useEffect(() => {
  // ...анимация
  
  return () => {
    cancelAnimationFrame(animationFrameRef.current);
  };
}, []);
```

**Результат:**
- Плавная анимация 60fps
- Автоматическая остановка при размонтировании
- Нет утечек памяти

---

## 📊 Сравнительная таблица

| Параметр | Обычные слои (PNG) | Анимация пешеходов (SVG) |
|----------|-------------------|--------------------------|
| **Формат** | PNG изображение | SVG компонент |
| **Путь** | `/genplan/*.png` | Компонент React |
| **Анимация** | Нет | Да (requestAnimationFrame) |
| **z-index** | 2 | 3 |
| **Прозрачность** | 0.85 | 0.9 |
| **Интерактивность** | pointer-events: none | pointer-events: none |
| **Cleanup** | - | cancelAnimationFrame |

---

## 📁 Измененные/созданные файлы

1. ✅ **`src/components/PedestrianAnimation.jsx`** - создан
   - SVG анимация
   - requestAnimationFrame
   - useEffect cleanup
   - 5 маршрутов с пешеходами

2. ✅ **`src/pages/Landscaping.jsx`** - обновлен
   - Импорт PedestrianAnimation
   - Условный рендеринг
   - Интеграция в систему слоёв

---

## 🎯 Результат

**Кнопка "Пути пешеходов" теперь активирует:**
- ✅ Анимированный SVG слой
- ✅ 5 маршрутов движения
- ✅ Движущиеся цветные пешеходы
- ✅ Пульсация и тени
- ✅ Плавное появление/исчезновение
- ✅ Автоматическая очистка
- ✅ Производительная анимация (60fps)

**Все в едином неоморфном стиле!** 🚶‍♂️✨

