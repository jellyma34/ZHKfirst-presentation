# 📦 Инструкция по созданию бекапа проекта

## 🚀 Быстрый старт

### Вариант 1: Bat-файл (рекомендуется для Windows)
```bash
# Двойной клик на файл
create-backup.bat
```

### Вариант 2: PowerShell скрипт
```powershell
# Запустить в PowerShell
.\create-backup.ps1
```

### Вариант 3: Ручная команда
```powershell
$date = Get-Date -Format "yyyy-MM-dd_HHmm"
Compress-Archive -Path src,PUBLIC,package.json,vite.config.js,tailwind.config.js,postcss.config.js,index.html -DestinationPath "backup-$date.zip" -Force
```

---

## 📋 Что включается в бекап

### ✅ Включено:
- `src/` - весь исходный код
- `PUBLIC/` - все медиа-файлы (изображения, видео)
- `package.json` - зависимости проекта
- `package-lock.json` - точные версии пакетов
- `vite.config.js` - конфигурация Vite
- `tailwind.config.js` - конфигурация Tailwind CSS
- `postcss.config.js` - конфигурация PostCSS
- `index.html` - главный HTML файл
- `README.md` - документация
- Все `.md` файлы - вся документация проекта

### ❌ Исключено:
- `node_modules/` - зависимости (восстанавливаются через `npm install`)
- `dist/` - билд (пересобирается через `npm run build`)
- `*.zip` - старые бекапы

---

## 🔄 Восстановление из бекапа

### 1. Распакуйте архив
```bash
# Windows Explorer: правый клик → Извлечь всё
# Или PowerShell:
Expand-Archive -Path backup-2025-10-07_0815.zip -DestinationPath .\restored-project
```

### 2. Установите зависимости
```bash
cd restored-project
npm install
```

### 3. Запустите проект
```bash
npm run dev
```

---

## 📊 Размер бекапа

**Приблизительный размер:**
- С PUBLIC/ (все медиа): ~50-100 MB
- Только код (без PUBLIC/): ~1-5 MB

**Для минимального бекапа только кода:**
```powershell
Compress-Archive -Path src,package.json,vite.config.js,tailwind.config.js,index.html -DestinationPath "backup-code-only.zip"
```

---

## 🛠️ Дополнительные опции

### Бекап с включением node_modules (полный):
```powershell
# ⚠️ ВНИМАНИЕ: Очень большой размер (200+ MB)
Compress-Archive -Path * -DestinationPath "full-backup.zip" -Force
```

### Бекап только PUBLIC папки:
```powershell
Compress-Archive -Path PUBLIC -DestinationPath "media-backup.zip"
```

### Бекап только исходного кода:
```powershell
Compress-Archive -Path src -DestinationPath "source-code-backup.zip"
```

---

## 📅 Рекомендации

1. **Регулярность:** Создавайте бекап перед крупными изменениями
2. **Хранение:** Храните бекапы в облаке (Google Drive, Dropbox) или на внешнем диске
3. **Версионирование:** Git - лучшая система для версионирования кода
4. **Автоматизация:** Настройте Git для автоматических коммитов

---

## 🔧 Альтернатива: Git

### Инициализация Git репозитория:
```bash
git init
git add .
git commit -m "Initial commit: Architectural website"
```

### Создание бекапа через Git:
```bash
git bundle create backup-$(date +%Y%m%d).bundle --all
```

---

## ✅ Проверка целостности бекапа

После создания бекапа:
1. Проверьте, что файл `.zip` создан
2. Проверьте размер файла (должен быть > 10 MB)
3. Попробуйте открыть архив и проверить содержимое
4. Протестируйте восстановление на другой машине

---

## 🆘 Troubleshooting

### Проблема: "Архив слишком большой"
**Решение:** Исключите папку PUBLIC если нужен только код

### Проблема: "Процесс долго выполняется"
**Решение:** Используйте `-CompressionLevel Fastest` вместо `Optimal`

### Проблема: "Ошибка доступа"
**Решение:** Запустите PowerShell от имени администратора

---

## 📞 Контакты

При проблемах с бекапом проверьте:
- ✅ Все файлы закрыты в редакторе
- ✅ Нет запущенных процессов npm/vite
- ✅ Достаточно места на диске

