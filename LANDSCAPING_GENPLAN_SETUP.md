# Интерактивный генплан благоустройства

## ✅ Что было создано

### 🎯 Новая секция на странице "Благоустройство"

Добавлена интерактивная секция с генпланом, аналогичная разделу "Планировочные решения".

---

## 📐 Структура компонента

### 1️⃣ Состояние и функции

**Добавлено:**
```jsx
const [activeLayers, setActiveLayers] = useState([]);

const toggleLayer = (layer) => {
  setActiveLayers((prev) =>
    prev.includes(layer)
      ? prev.filter((l) => l !== layer)
      : [...prev, layer]
  );
};

const availableLayers = [
  { id: 'озеленение', label: 'Озеленение', icon: TreePine },
  { id: 'детские-площадки', label: 'Детские площадки', icon: Sun },
  { id: 'освещение', label: 'Освещение', icon: Zap },
  { id: 'парковки', label: 'Парковки', icon: MapPin },
  { id: 'пешеходные-зоны', label: 'Пешеходные зоны', icon: ArrowRight },
  { id: 'зоны-отдыха', label: 'Зоны отдыха', icon: Flower }
];
```

### 2️⃣ Трехколоночный Layout

**Структура:**
```jsx
<div className="landscaping-layout" style={{
  display: 'grid',
  gridTemplateColumns: 'minmax(280px, 1fr) minmax(900px, 1.8fr) minmax(280px, 1fr)',
  alignItems: 'start',
  gap: '3rem',
  padding: '2rem 3rem',
  maxWidth: '1920px',
  margin: '0 auto'
}}>
  {/* Левая панель - пустое пространство */}
  {/* Центр - генплан */}
  {/* Правая панель - фильтры */}
</div>
```

### 3️⃣ Центральный блок с генпланом

**Характеристики:**
```jsx
transform: 'scale(0.65)'          // Масштаб 65%
transformOrigin: 'center top'     // От верха
background: 'transparent'         // Без фона
border: 'none'                    // Без рамки
boxShadow: 'none'                 // Без теней
maxWidth: '1400px'                // Максимальная ширина
```

**Изображение:**
- Путь: `/plans/general/main.jpg` (временно)
- Чистый дизайн без декораций
- Fade-in анимация

### 4️⃣ Система наложения слоёв

**Слои позиционированы:**
```jsx
position: 'absolute'
top: '50%'
left: '50%'
transform: 'translate(-50%, -50%)'
opacity: 0.85
zIndex: 2
```

**Путь к слоям:**
```
/plans/genplan/озеленение.png
/plans/genplan/детские-площадки.png
/plans/genplan/освещение.png
/plans/genplan/парковки.png
/plans/genplan/пешеходные-зоны.png
/plans/genplan/зоны-отдыха.png
```

### 5️⃣ Панель фильтров слоёв

**Стили:**
- Glassmorphism: `rgba(255, 255, 255, 0.75) + blur(8px)`
- Sticky позиционирование: `top: 3rem`
- Неоморфные кнопки с hover эффектами
- Индикатор активных слоёв

**Особенности:**
- Иконки для каждого слоя
- Визуальная индикация активных слоёв
- Счетчик активных слоёв внизу
- Плавные transitions

---

## 📁 Необходимые файлы

### Создайте папку и добавьте изображения:

```
PUBLIC/plans/genplan/
  ├── основное.jpg                    ← Основное изображение генплана
  ├── озеленение.png                  ← Слой с зонами озеленения
  ├── детские-площадки.png            ← Слой с детскими площадками
  ├── освещение.png                   ← Слой с освещением
  ├── парковки.png                    ← Слой с парковочными местами
  ├── пешеходные-зоны.png            ← Слой с пешеходными дорожками
  └── зоны-отдыха.png                ← Слой с зонами отдыха
```

### Временное решение:

Сейчас используется `/plans/general/main.jpg` как placeholder. Когда будут готовы файлы генплана, обновите путь в коде:

```jsx
// Строка 259 в Landscaping.jsx
src="/plans/genplan/основное.jpg"  // Вместо /plans/general/main.jpg
```

---

## 🎨 Визуальная структура

```
┌──────────────────────────────────────────────────┐
│     ГЕНЕРАЛЬНЫЙ ПЛАН БЛАГОУСТРОЙСТВА             │
│                                                  │
│  ┌─────────┐  ┌───────────────────┐  ┌────────┐ │
│  │         │  │                   │  │Фильтры │ │
│  │ Пустое  │  │   ГЕНПЛАН (65%)  │  │слоёв:  │ │
│  │ место   │  │                   │  │        │ │
│  │ для     │  │   + Слои поверх   │  │☐ Озеле │ │
│  │ баланса │  │                   │  │☐ Детск │ │
│  │         │  │   Чистый дизайн   │  │☐ Освещ │ │
│  │         │  │   Без рамок       │  │☐ Парко │ │
│  │         │  │                   │  │☐ Пешех │ │
│  │         │  │                   │  │☐ Зоны  │ │
│  └─────────┘  └───────────────────┘  └────────┘ │
│      1fr            1.8fr                1fr     │
│                                                  │
│          gap: 3rem         gap: 3rem            │
└──────────────────────────────────────────────────┘
```

---

## ✨ Функциональность

### Панель фильтров:

1. **Кнопка неактивна:**
   - Фон: `rgba(255, 255, 255, 0.9)`
   - Тени: выпуклые (raised)
   - Иконка серая

2. **Кнопка активна:**
   - Фон: `rgba(248, 249, 251, 0.95)`
   - Тени: вдавленные (inset)
   - Иконка акцентная (#5F758D)
   - Обводка 2px

3. **Hover эффект:**
   - Scale: 1.02
   - Усиленные тени

### Наложение слоёв:

- Центрирование через `translate(-50%, -50%)`
- Прозрачность: 0.85
- Плавное появление/исчезновение
- Z-index: 2 (поверх основного изображения)

---

## 🚀 Как использовать

### 1. Подготовьте изображения:

**Основное изображение:**
- Генплан благоустройства территории
- Формат: JPG
- Имя: `основное.jpg`

**Слои (PNG с прозрачным фоном):**
- Каждый слой - отдельный элемент благоустройства
- Формат: PNG (с альфа-каналом)
- Размер: идентичный основному изображению
- Прозрачные области, где не нужно показывать элемент

### 2. Разместите файлы:

```
PUBLIC/plans/genplan/
  ├── основное.jpg              ← Генплан
  ├── озеленение.png            ← Зеленые зоны
  ├── детские-площадки.png      ← Детские площадки
  ├── освещение.png             ← Фонари и освещение
  ├── парковки.png              ← Парковочные зоны
  ├── пешеходные-зоны.png       ← Дорожки
  └── зоны-отдыха.png           ← Скамейки, беседки
```

### 3. Обновите путь в коде (если нужно):

```jsx
// src/pages/Landscaping.jsx, строка ~259
src="/plans/genplan/основное.jpg"
```

### 4. Протестируйте:

1. Откройте страницу "Благоустройство"
2. Прокрутите до секции "Генеральный план"
3. Нажимайте на фильтры слоёв
4. Наблюдайте наложение слоёв на генплан

---

## 📱 Адаптивность

| Размер экрана | Layout | Масштаб генплана |
|---------------|--------|------------------|
| **≥1024px** | 3 колонки | 65% |
| **≤1024px** | 1 колонка | 100% |
| **≤768px** | 1 колонка | 100% |

На планшетах и мобильных:
- Layout переключается в вертикальный
- Генплан в обычном масштабе (100%)
- Панель фильтров под генпланом

---

## 🎨 Стили

**Файл:** `src/styles/landscaping.css`

**Включает:**
- ✅ Fade-in анимация для генплана
- ✅ Кастомный scrollbar для панели
- ✅ Адаптивные медиа-запросы
- ✅ Hover эффекты

---

## 📊 Консольное логирование

При работе со слоями в консоли отображается:

```
✅ Генплан загружен
✅ Включен слой: озеленение
✅ Слой загружен: озеленение
❌ Отключен слой: озеленение
```

При ошибках:
```
❌ Ошибка загрузки генплана
❌ Ошибка загрузки слоя: парковки
```

---

## 🔧 Настройка

### Добавление новых слоёв:

Обновите массив `availableLayers`:

```jsx
const availableLayers = [
  { id: 'новый-слой', label: 'Новый слой', icon: IconName },
  // ...
];
```

Добавьте соответствующий файл:
```
PUBLIC/plans/genplan/новый-слой.png
```

### Изменение масштаба по умолчанию:

```jsx
// В строке 242
transform: 'scale(0.8)'  // Например, 80%
```

---

## 📁 Измененные файлы

1. ✅ **`src/pages/Landscaping.jsx`**
   - Добавлено состояние `activeLayers`
   - Добавлена функция `toggleLayer`
   - Добавлен массив `availableLayers`
   - Добавлена новая секция с генпланом
   - Добавлена панель фильтров слоёв

2. ✅ **`src/styles/landscaping.css`** (создан)
   - Fade-in анимация
   - Кастомный scrollbar
   - Адаптивные стили

---

## 🎯 Следующие шаги

1. **Создайте папку:**
   ```
   PUBLIC/plans/genplan/
   ```

2. **Добавьте основное изображение:**
   ```
   PUBLIC/plans/genplan/основное.jpg
   ```

3. **Добавьте слои (PNG с прозрачностью):**
   ```
   PUBLIC/plans/genplan/озеленение.png
   PUBLIC/plans/genplan/детские-площадки.png
   PUBLIC/plans/genplan/освещение.png
   PUBLIC/plans/genplan/парковки.png
   PUBLIC/plans/genplan/пешеходные-зоны.png
   PUBLIC/plans/genplan/зоны-отдыха.png
   ```

4. **Обновите путь в коде** (строка 259):
   ```jsx
   src="/plans/genplan/основное.jpg"
   ```

5. **Скопируйте в dist:**
   ```
   dist/plans/genplan/
   ```

---

## 🎨 Премиум-функции

✅ **Glassmorphism** - полупрозрачная панель с размытием  
✅ **Sticky positioning** - панель остается видимой при скролле  
✅ **Неоморфные кнопки** - мягкие тени и transitions  
✅ **Иконки для слоёв** - визуальная идентификация  
✅ **Fade-in анимации** - плавное появление элементов  
✅ **Responsive design** - адаптивный layout  
✅ **Консольное логирование** - отладка загрузки  

---

**Структура готова! Осталось только добавить изображения генплана.** 🚀

