# 🎠 Слайдер с видимыми соседними карточками

## ✅ Реализовано

Главная страница обновлена: вместо одиночной карточки теперь **слайдер с 3D-эффектом**, где видна центральная карточка и уменьшенные соседние по бокам.

---

## 🎯 Основные возможности

### 1. **3D-слайдер**
- **Центральная карточка**: в фокусе, полного размера (scale: 1)
- **Левая карточка**: уменьшена до 80%, смещена влево (-70%), полупрозрачна (opacity: 0.7)
- **Правая карточка**: уменьшена до 80%, смещена вправо (+70%), полупрозрачна (opacity: 0.7)
- **Скрытые карточки**: минимальны (scale: 0.5), невидимы (opacity: 0)

### 2. **Навигация**
- **Кнопки ❮ / ❯**: ручное переключение слайдов
- **Автопрокрутка**: каждые 6 секунд
- **Кнопка управления**: ⏸ Остановить / ▶ Воспроизвести

### 3. **Анимация**
- Плавные переходы: `transition: all 0.8s ease`
- Smooth scaling и transform
- Градиентный overlay внизу карточки для читаемости текста

---

## 📂 Структура файлов

### 1. **`src/pages/HomePage.jsx`**
Основной компонент с логикой слайдера:

```jsx
const slides = [
  {
    title: "Информация об объекте капитального строительства",
    description: "Тип объекта: Жилой комплекс...",
    image: "/images/Фасадные решения-1.png"
  },
  // ... 4 карточки
];

const [index, setIndex] = useState(0);
const [isPlaying, setIsPlaying] = useState(true);
```

**Логика позиционирования карточек:**
```jsx
const offset = (i - index + slides.length) % slides.length;
let positionClass = "";
if (offset === 0) positionClass = "center";      // Центральная
else if (offset === 1 || ...) positionClass = "right";  // Правая
else if (offset === slides.length - 1 || ...) positionClass = "left";  // Левая
else positionClass = "hidden";                   // Скрытая
```

### 2. **`src/styles/Home.css`**
Стили для слайдера:

**Контейнер:**
```css
.slider {
  position: relative;
  width: 90%;
  max-width: 1100px;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

**Карточки:**
```css
.slide {
  position: absolute;
  width: 55%;
  height: 100%;
  transition: all 0.8s ease;
  box-shadow: 0 10px 25px rgba(0,0,0,0.3);
}
```

**Позиции:**
```css
.slide.center {
  transform: translateX(0) scale(1);
  opacity: 1;
  z-index: 3;
}

.slide.left {
  transform: translateX(-70%) scale(0.8);
  opacity: 0.7;
  z-index: 2;
}

.slide.right {
  transform: translateX(70%) scale(0.8);
  opacity: 0.7;
  z-index: 2;
}

.slide.hidden {
  opacity: 0;
  transform: scale(0.5);
  z-index: 1;
}
```

---

## 🎨 Визуальный дизайн

### Цветовая схема
- **Фон**: Градиент `#f5f7fa → #c3cfe2`
- **Карточки**: Белые с тенью и скругленными углами
- **Градиент на карточке**: `rgba(0,0,0,0.9) → transparent`
- **Кнопки**: Белые полупрозрачные с hover-эффектами

### Типографика
- **Заголовок**: Manrope, 2rem, bold
- **Описание**: Inter, 1rem, regular
- **Текст карточки**: Manrope, 1.5rem (заголовок), 0.95rem (описание)

---

## 🔧 Функционал

### Автоматическая прокрутка
```jsx
useEffect(() => {
  if (isPlaying) {
    intervalRef.current = setInterval(() => {
      setIndex((prev) => (prev + 1) % slides.length);
    }, 6000);
  } else {
    if (intervalRef.current) {
      clearInterval(intervalRef.current);
    }
  }
  return () => {
    if (intervalRef.current) {
      clearInterval(intervalRef.current);
    }
  };
}, [isPlaying, slides.length]);
```

### Навигация
```jsx
const nextSlide = () => setIndex((prev) => (prev + 1) % slides.length);
const prevSlide = () => setIndex((prev) => (prev - 1 + slides.length) % slides.length);
```

### Управление воспроизведением
```jsx
<button className="play-toggle" onClick={() => setIsPlaying(!isPlaying)}>
  {isPlaying ? "⏸ Остановить" : "▶ Воспроизвести"}
</button>
```

---

## 📱 Адаптивность

### Desktop (> 1024px)
- Слайдер: max-width 1100px
- Карточки: ширина 55%
- Видны 3 карточки (центр + 2 боковые)

### Tablet (768px - 1024px)
- Слайдер: max-width 900px
- Карточки: ширина 60%
- Боковые карточки: scale 0.75, смещение 75%

### Mobile (< 768px)
- Слайдер: width 100%
- Карточки: ширина 75%
- **Боковые карточки скрыты** (opacity: 0)
- Видна только центральная карточка
- Высота карточек: 400px → 350px (< 480px)

---

## 🖼️ Изображения

Путь к изображениям: `public/images/`

**5 изображений:**
1. Фасадные решения-1.png
2. Фасадные решения-2.png
3. Фасадные решения-3.png
4. Фасадные решения-4.png
5. Фасадные решения-5.png

---

## 🎭 Детали реализации

### Gradient Overlay
```css
.slide-info {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  padding: 30px;
  background: linear-gradient(to top, rgba(0,0,0,0.9), rgba(0,0,0,0));
  color: #fff;
}
```

### Стрелки навигации
```css
.arrow {
  background: rgba(255,255,255,0.9);
  width: 50px;
  height: 50px;
  border-radius: 50%;
  font-size: 2rem;
  transition: all 0.3s ease;
}

.arrow:hover {
  background: #fff;
  transform: scale(1.1);
}
```

### Кнопка управления
```css
.play-toggle {
  padding: 12px 30px;
  background: rgba(255,255,255,0.9);
  border-radius: 30px;
  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
}

.play-toggle:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0,0,0,0.25);
}
```

---

## 🚀 Запуск

```bash
npm run dev
```

Откройте браузер и перейдите на главную страницу.

---

## ✨ Особенности

✅ **3D-эффект** — центральная карточка в фокусе, соседние видны  
✅ **Плавные анимации** — 0.8s ease transitions  
✅ **Автопрокрутка** — каждые 6 секунд  
✅ **Управление воспроизведением** — кнопка паузы/воспроизведения  
✅ **Ручная навигация** — стрелки влево/вправо  
✅ **Адаптивность** — отлично работает на всех устройствах  
✅ **Градиентный overlay** — читаемость текста на любом фоне  
✅ **Hover-эффекты** — интерактивные кнопки  

---

**Дата создания**: 2025-10-07  
**Версия**: 1.0  
**Статус**: ✅ Завершено

