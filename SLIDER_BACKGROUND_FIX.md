# 🔧 Исправление фона слайдера и позиционирования стрелок

## ✅ Что исправлено

### 1. **Убран белый фон/квадрат**
Убран нежелательный белый фон, который появлялся позади карточек слайдера.

### 2. **Стрелки вынесены дальше**
Стрелки переключения (❮ и ❯) теперь расположены еще дальше от карточек и не перекрывают их.

---

## 🔧 Внесенные изменения

### 1. Контейнер слайдера (`.slider`)
```css
.slider {
  /* ... */
  background: none; /* убираем белый фон */
  box-shadow: none; /* чтобы убрать белый квадрат */
}
```

### 2. Трек слайдов (`.slide-track`)
```css
.slide-track {
  /* ... */
  overflow: visible; /* чтобы стрелки не обрезались */
  background: none; /* убираем белый фон */
}
```

### 3. Стрелки переключения (`.arrow`)
```css
.arrow {
  position: absolute;
  top: 50%; /* вертикальное центрирование */
  transform: translateY(-50%); /* точное центрирование */
  width: 60px;
  height: 60px;
  z-index: 20; /* выше других элементов */
  /* ... */
}

.arrow:hover {
  transform: translateY(-50%) scale(1.1); /* сохраняем центрирование при hover */
}

.arrow.left {
  left: -120px; /* было -90px */
}

.arrow.right {
  right: -120px; /* было -90px */
}
```

---

## 📐 Расположение стрелок

### Desktop (> 1024px)
```
     -120px                                      -120px
        ❮                                           ❯
           ┌────────────────────────────┐
           │  [Левая] [ЦЕНТР] [Правая]  │
           └────────────────────────────┘
```

### Tablet (768px - 1024px)
```
    -80px                                    -80px
      ❮                                        ❯
         ┌──────────────────────────┐
         │  [Левая] [ЦЕНТР] [Правая]  │
         └──────────────────────────┘
```

### Mobile (< 768px)
```
   10px                                          10px
    ❮                                             ❯
      ┌──────────────────────────────┐
      │        [Только центр]         │
      └──────────────────────────────┘
```

---

## 📊 Размеры стрелок

| Экран | Размер | Left | Right |
|-------|--------|------|-------|
| Desktop (>1024px) | 60x60px | -120px | -120px |
| Tablet (768-1024px) | 56x56px | -80px | -80px |
| Mobile (<768px) | 50x50px | 10px | 10px |

---

## ✨ Улучшения

✅ **Убран белый фон** — слайдер теперь прозрачный  
✅ **Стрелки не перекрывают карточки** — вынесены на -120px  
✅ **Точное вертикальное центрирование** — `top: 50%` + `transform: translateY(-50%)`  
✅ **Увеличенный размер** — 60x60px для лучшей видимости  
✅ **Сохранение центрирования при hover** — `transform: translateY(-50%) scale(1.1)`  
✅ **Повышенный z-index** — z-index: 20 для гарантированного отображения поверх других элементов  

---

## 🎨 Визуальные особенности

### До
- Белый фон/квадрат позади карточек ❌
- Стрелки на -90px (ближе к карточкам) ❌
- Размер 55x55px ❌

### После
- Полностью прозрачный фон ✅
- Стрелки на -120px (дальше от карточек) ✅
- Размер 60x60px ✅
- Вертикальное центрирование с помощью transform ✅

---

## 🔍 Детали реализации

### Центрирование стрелок
```css
/* Точное вертикальное центрирование относительно контейнера */
.arrow {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
}

/* При hover сохраняем центрирование */
.arrow:hover {
  transform: translateY(-50%) scale(1.1);
}

/* При active также сохраняем */
.arrow:active {
  transform: translateY(-50%) scale(0.95);
}
```

### Прозрачность контейнеров
```css
/* Убираем любой фон */
.slider {
  background: none;
  box-shadow: none;
}

.slide-track {
  background: none;
  overflow: visible; /* стрелки видны за пределами */
}
```

---

## 📱 Адаптивность

### Общие принципы
1. **Desktop**: стрелки максимально далеко (-120px) для чистого вида
2. **Tablet**: стрелки ближе (-80px) для удобства
3. **Mobile**: стрелки внутри контейнера (10px) для видимости на маленьких экранах

---

**Файл**: `src/styles/Home.css`  
**Дата**: 2025-10-07  
**Статус**: ✅ Исправлено

