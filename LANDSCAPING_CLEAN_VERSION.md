# Чистая версия раздела "Благоустройство"

## ✅ Что было сделано

### 🎯 Цель
Оставить на странице только два элемента:
1. Центральный блок с изображением генплана
2. Панель фильтров слоёв (справа)

Все остальное удалено.

---

## 🗑️ Что было удалено

### Удаленные секции:

1. ❌ **Hero Section** - полноэкранный баннер с фото
2. ❌ **Floating elements** - анимированные иконки
3. ❌ **Interactive Sections** - вкладки с направлениями работы
4. ❌ **Projects Portfolio** - портфолио проектов
5. ❌ **Process Section** - этапы работы

### Удаленные зависимости:

```jsx
- useScroll, useTransform (не используются)
- useEffect, useRef (не нужны)
- AnimatePresence (не используется)
- Иконки: Droplets, Sun, Moon, Clock (удалены)
- sections, projects массивы (удалены)
- activeSection состояние (удалено)
```

---

## 🎨 Новая структура

### Упрощенный импорт:

```jsx
import React, { useState } from 'react';
import { motion } from 'framer-motion';
import '../styles/landscaping.css';
```

### Состояние:

```jsx
const [activeLayers, setActiveLayers] = useState([]);
```

### Функция переключения:

```jsx
const toggleLayer = (layer) => {
  setActiveLayers((prev) =>
    prev.includes(layer)
      ? prev.filter((l) => l !== layer)
      : [...prev, layer]
  );
  console.log(`${activeLayers.includes(layer) ? '❌ Отключен' : '✅ Включен'} слой: ${layer}`);
};
```

---

## 📐 Layout структура

### Flex Layout (2 элемента):

```jsx
<div className="landscaping-layout" style={{
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'flex-start',
  gap: '3rem',
  padding: '3rem 5rem',
  maxWidth: '1920px',
  margin: '0 auto'
}}>
  {/* Генплан (слева) */}
  {/* Фильтры (справа) */}
</div>
```

**Структура:**
```
┌─────────────────────────────────────┐
│                                     │
│  ┌──────────────┐   ┌──────────┐   │
│  │              │   │ Фильтры  │   │
│  │  ГЕНПЛАН     │   │ слоёв    │   │
│  │  (65%)       │   │          │   │
│  │              │   │ □ Озелен │   │
│  │              │   │ □ Площад │   │
│  │              │   │ □ Освещ  │   │
│  │              │   │ □ Парков │   │
│  │              │   │ □ Пешех  │   │
│  └──────────────┘   │ □ Зоны   │   │
│                     └──────────┘   │
│                                     │
│     gap: 3rem                       │
└─────────────────────────────────────┘
```

---

## 🖼️ Центральный блок с генпланом

### Стили контейнера:

```jsx
style={{
  position: 'relative',
  background: 'transparent',      // ✅ Без фона
  border: 'none',                 // ✅ Без рамки
  boxShadow: 'none',              // ✅ Без теней
  transform: 'scale(0.65)',       // ✅ Масштаб 65%
  transformOrigin: 'center top',  // ✅ От верха
  transition: 'transform 0.3s ease',
  alignSelf: 'flex-start',
  width: '100%',
  maxWidth: '1400px'
}}
```

### Основное изображение:

```jsx
<img
  src="/plans/general/main.jpg"    // ← ВРЕМЕННЫЙ путь
  alt="Генплан благоустройства"
  style={{
    width: '100%',
    height: 'auto',
    objectFit: 'contain',
    borderRadius: 0,                // ✅ Без скругления
    boxShadow: 'none',              // ✅ Без теней
    border: 'none',                 // ✅ Без рамки
    position: 'relative',
    zIndex: 1
  }}
/>
```

**Обработка ошибок:**
- При загрузке: консольный лог
- При ошибке: fallback на placeholder

### Система слоёв:

```jsx
{activeLayers.map((layer) => (
  <motion.img
    key={layer}
    src={`/plans/genplan/${layer}.png`}
    alt={layer}
    style={{
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%',
      objectFit: 'contain',
      opacity: 0.9,              // ✅ Прозрачность 90%
      zIndex: 2,
      pointerEvents: 'none',
      mixBlendMode: 'normal'
    }}
    initial={{ opacity: 0 }}
    animate={{ opacity: 0.9 }}
    exit={{ opacity: 0 }}
    transition={{ duration: 0.3 }}
  />
))}
```

---

## 🎛️ Панель фильтров слоёв

### Glassmorphism стиль:

```jsx
style={{
  background: 'rgba(255, 255, 255, 0.75)',
  borderRadius: '30px',
  backdropFilter: 'blur(10px)',
  boxShadow: 'inset 5px 5px 10px rgba(0, 0, 0, 0.05), 
              inset -5px -5px 12px rgba(255, 255, 255, 1)',
  padding: '1.8rem',
  width: '320px',
  position: 'sticky',
  top: '3rem'
}}
```

### Список фильтров:

```jsx
[
  'Озеленение',
  'Детские площадки',
  'Освещение',
  'Парковки',
  'Пешеходные зоны',
  'Зоны отдыха'
]
```

### Стили кнопок:

**Неактивная:**
```jsx
background: 'white'
boxShadow: '2px 2px 6px rgba(0, 0, 0, 0.05), 
            -2px -2px 6px rgba(255, 255, 255, 0.8)'
```

**Активная:**
```jsx
background: '#dde3f0'
boxShadow: 'inset 2px 2px 4px rgba(0, 0, 0, 0.1), 
            inset -2px -2px 4px rgba(255, 255, 255, 0.9)'
border: '1px solid rgba(95, 117, 141, 0.2)'
color: '#5F758D'
```

**Hover (для неактивных):**
```jsx
boxShadow: '3px 3px 8px rgba(0, 0, 0, 0.08), 
            -3px -3px 8px rgba(255, 255, 255, 1)'
```

### Информация об активных слоях:

Внизу панели отображается:
```
Активные слои: Озеленение, Парковки
```

Или:
```
Активные слои: Нет
```

---

## 📁 Структура файлов

### Код компонента:

**src/pages/Landscaping.jsx** - полностью переписан

**Размер:** ~200 строк (было ~630)

**Импорты:** минимальные (только useState, motion)

### Стили:

**src/styles/landscaping.css** - существующий файл с:
- Fade-in анимация
- Кастомный scrollbar
- Адаптивные стили

---

## 🎨 Визуальный результат

### Полная страница:

```
┌─────────────────────────────────────────────────┐
│  [Navbar - не изменена]                         │
├─────────────────────────────────────────────────┤
│                                                 │
│        ГРАДИЕНТНЫЙ ФОН (#f9fafc → #eef1f6)     │
│                                                 │
│  ┌───────────────────────┐   ┌──────────────┐  │
│  │                       │   │  ФИЛЬТРЫ     │  │
│  │   ГЕНПЛАН (65%)       │   │  СЛОЁВ       │  │
│  │                       │   │              │  │
│  │   /plans/general/     │   │ □ Озеленение │  │
│  │   main.jpg            │   │ □ Площадки   │  │
│  │                       │   │ □ Освещение  │  │
│  │   + Слои поверх       │   │ □ Парковки   │  │
│  │                       │   │ □ Пешеходные │  │
│  │                       │   │ □ Зоны отдыха│  │
│  └───────────────────────┘   │              │  │
│                               │ Активные: 0  │  │
│                               └──────────────┘  │
│                                                 │
│  [Footer - не изменена]                         │
└─────────────────────────────────────────────────┘
```

---

## 🚀 Как использовать

### 1. Страница готова к использованию

Откройте страницу "Благоустройство" - увидите:
- Генплан (временно из /plans/general/main.jpg)
- Панель фильтров справа

### 2. Добавьте файлы генплана

**Создайте папку:**
```powershell
New-Item -Path "PUBLIC\plans\genplan" -ItemType Directory -Force
```

**Добавьте файлы:**
```
PUBLIC/plans/genplan/
  ├── основное.jpg               ← Основной генплан
  ├── Озеленение.png             ← Слой 1
  ├── Детские площадки.png       ← Слой 2
  ├── Освещение.png              ← Слой 3
  ├── Парковки.png               ← Слой 4
  ├── Пешеходные зоны.png        ← Слой 5
  └── Зоны отдыха.png            ← Слой 6
```

### 3. Обновите путь в коде (опционально)

Если хотите использовать genplan вместо general:

```jsx
// Строка 55 в Landscaping.jsx
- src="/plans/general/main.jpg"
+ src="/plans/genplan/основное.jpg"
```

### 4. Скопируйте в dist

```powershell
Copy-Item -Path "PUBLIC\plans\genplan" -Destination "dist\plans\" -Recurse -Force
```

---

## ✨ Функциональность

### Что работает:

1. ✅ **Отображение генплана** - основное изображение
2. ✅ **Фильтры слоёв** - 6 кнопок переключения
3. ✅ **Наложение слоёв** - прозрачные PNG поверх
4. ✅ **Визуальная индикация** - активные слои подсвечены
5. ✅ **Счетчик** - показывает количество активных слоёв
6. ✅ **Консольное логирование** - отладка
7. ✅ **Sticky панель** - остается видимой при скролле
8. ✅ **Hover эффекты** - неоморфные тени
9. ✅ **Fade-in анимации** - плавное появление

### Доступные фильтры:

- Озеленение
- Детские площадки
- Освещение
- Парковки
- Пешеходные зоны
- Зоны отдыха

---

## 📊 Сравнение До/После

| Параметр | Было | Стало |
|----------|------|-------|
| **Строк кода** | ~630 | ~200 |
| **Секций** | 5 | 1 |
| **Состояний** | 2 (activeSection, activeLayers) | 1 (activeLayers) |
| **Импортов** | 10+ | 3 |
| **Зависимостей motion** | useScroll, useTransform, AnimatePresence | motion |
| **Иконок** | 10+ | 0 (в кнопках) |
| **Hero баннер** | Да | ❌ Удален |
| **Портфолио** | Да | ❌ Удалено |
| **Этапы работы** | Да | ❌ Удалены |

---

## 🎨 Визуальные особенности

### Фон страницы:

```jsx
background: 'linear-gradient(180deg, #f9fafc 0%, #eef1f6 100%)'
paddingTop: '6rem'  // Отступ для navbar
```

### Генплан:

```
Масштаб: 65%
Без декораций (рамок, теней, фона)
Чистый минималистичный дизайн
Transform origin: center top
```

### Панель фильтров:

```
Glassmorphism эффект
Sticky позиционирование
Неоморфные кнопки
Индикатор активных слоёв
```

---

## 🧪 Как проверить

1. **Откройте страницу "Благоустройство"**

2. **Проверьте:**
   - ✅ Видна только секция с генпланом
   - ✅ Нет Hero баннера
   - ✅ Нет портфолио проектов
   - ✅ Нет этапов работы
   - ✅ Navbar и Footer работают

3. **Протестируйте фильтры:**
   - ✅ Кликайте на фильтры слоёв
   - ✅ Состояние меняется (вдавленная/выпуклая кнопка)
   - ✅ Счетчик внизу обновляется
   - ✅ Консоль показывает логи

4. **После добавления файлов genplan:**
   - ✅ Слои будут накладываться на генплан
   - ✅ Прозрачность 90%
   - ✅ Плавное появление/исчезновение

---

## 📁 Текущее состояние файлов

### 1. src/pages/Landscaping.jsx

**Размер:** ~200 строк  
**Статус:** ✅ Полностью переписан  
**Линтер:** ✅ Нет ошибок

**Содержит:**
- Состояние activeLayers
- Функция toggleLayer
- Flex layout с 2 элементами
- Генплан с системой слоёв
- Панель фильтров

### 2. src/styles/landscaping.css

**Статус:** ✅ Существует  
**Содержит:**
- Fade-in анимация
- Кастомный scrollbar
- Адаптивные медиа-запросы

### 3. PUBLIC/plans/genplan/

**Статус:** ⚠️ Нужно создать  
**Требуется:**
- основное.jpg
- Озеленение.png
- Детские площадки.png
- Освещение.png
- Парковки.png
- Пешеходные зоны.png
- Зоны отдыха.png

---

## 🎯 Следующие шаги

### Для полной функциональности:

1. **Создайте папку genplan:**
   ```powershell
   New-Item -Path "PUBLIC\plans\genplan" -ItemType Directory -Force
   ```

2. **Добавьте изображения:**
   - Основное: основное.jpg
   - Слои: PNG с прозрачностью

3. **Обновите путь в коде:**
   ```jsx
   src="/plans/genplan/основное.jpg"
   ```

4. **Скопируйте в dist:**
   ```powershell
   Copy-Item -Path "PUBLIC\plans\genplan" -Destination "dist\plans\" -Recurse -Force
   ```

---

## ✅ Преимущества чистой версии

1. 🎯 **Фокус** - только на генплане и фильтрах
2. 🧹 **Простота** - минимум кода и зависимостей
3. ⚡ **Производительность** - меньше рендеринга
4. 🔧 **Поддержка** - проще модифицировать
5. 📱 **Адаптивность** - легче адаптировать под устройства
6. 🎨 **Чистота** - минималистичный дизайн

---

## 🎉 Готово!

**Страница "Благоустройство" полностью очищена!**

Остались только:
- ✅ Центральный блок с генпланом (65%)
- ✅ Панель фильтров слоёв (справа)
- ✅ Система наложения слоёв
- ✅ Чистый минималистичный дизайн

**Навигация и сайт не изменены!** 🌳✨

