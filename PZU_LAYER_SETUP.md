# Настройка слоя ПЗУ (Правила землепользования)

## ✅ Что было сделано в коде

### 1️⃣ Добавлена кнопка "ПЗУ" в фильтры

**Обновлен массив фильтров:**
```jsx
// src/pages/Landscaping.jsx, строка 138-144
[
  'Озеленение',
  'Детские площадки',
  'Освещение',
  'Парковки',
  'Пешеходные зоны',
  'Зоны отдыха',
  'ПЗУ'  // ✅ Добавлено
]
```

### 2️⃣ Настроена специальная обработка пути для ПЗУ

**Добавлена логика:**
```jsx
// src/pages/Landscaping.jsx, строки 80-83
{activeLayers.map((layer) => {
  // Для ПЗУ используем специальный путь
  const layerPath = layer === 'ПЗУ' 
    ? '/plans/genplan/2.png'      // ✅ Специальный путь
    : `/genplan/${layer}.png`;    // Для остальных слоёв
```

### 3️⃣ Обновлена прозрачность слоя

**Прозрачность:**
```jsx
animate={{ opacity: 0.85 }}  // Было: 0.9
```

### 4️⃣ Уменьшен масштаб генплана

**Масштаб:**
```jsx
transform: 'scale(0.52)'  // Было: 0.65
```

---

## 📁 Необходимые файлы

### ✅ Файл существует:

```
PUBLIC/genplan/2.png  ✅ Существует
```

**Изображение:** Цветной план благоустройства с зонами (озеленение, парковки, площадки).

### ⚠️ Нужно скопировать:

Файл должен быть доступен по пути `/plans/genplan/2.png` в браузере.

**Выполните вручную:**

#### Windows Explorer:

1. Откройте папку:
   ```
   C:\Users\maris_uf1mlhr\Downloads\Новая папка (15)\PUBLIC\genplan\
   ```

2. Скопируйте файл `2.png`

3. Создайте папку (если не существует):
   ```
   C:\Users\maris_uf1mlhr\Downloads\Новая папка (15)\PUBLIC\plans\genplan\
   ```

4. Вставьте файл `2.png` в эту папку

#### PowerShell (из корня проекта):

```powershell
# Создать папку
New-Item -ItemType Directory -Path "PUBLIC\plans\genplan" -Force

# Скопировать файл
Copy-Item -Path "PUBLIC\genplan\2.png" -Destination "PUBLIC\plans\genplan\2.png" -Force
```

#### CMD (из корня проекта):

```cmd
mkdir PUBLIC\plans\genplan
copy PUBLIC\genplan\2.png PUBLIC\plans\genplan\2.png
```

---

## 🎨 Как работает слой ПЗУ

### Логика в коде:

```jsx
const layerPath = layer === 'ПЗУ' 
  ? '/plans/genplan/2.png'           // ПЗУ → 2.png
  : `/genplan/${layer}.png`;         // Другие → название.png

Примеры:
- 'ПЗУ'            → '/plans/genplan/2.png'
- 'Озеленение'     → '/genplan/Озеленение.png'
- 'Парковки'       → '/genplan/Парковки.png'
```

### Визуальный эффект:

```
Генплан (основное.jpg)
     ↓
Клик на "ПЗУ"
     ↓
Наложение 2.png (opacity: 0.85)
     ↓
┌──────────────────────┐
│  [Генплан]           │
│    + [ПЗУ слой]      │ ← Цветные зоны поверх
│                      │   (прозрачность 85%)
│  Видны зоны ПЗУ      │
└──────────────────────┘
```

---

## 🧪 Как проверить

### 1. Скопируйте файл:

```
Источник: PUBLIC/genplan/2.png
Назначение: PUBLIC/plans/genplan/2.png
```

### 2. Откройте страницу "Благоустройство"

### 3. Найдите панель "Фильтры слоёв"

Должна быть кнопка **"ПЗУ"** внизу списка:
```
☐ Озеленение
☐ Детские площадки
☐ Освещение
☐ Парковки
☐ Пешеходные зоны
☐ Зоны отдыха
☐ ПЗУ  ← НОВАЯ КНОПКА
```

### 4. Кликните на "ПЗУ"

**Ожидаемое поведение:**
- ✅ Кнопка меняет стиль (вдавленная, accent цвет)
- ✅ Слой `2.png` накладывается на генплан
- ✅ Прозрачность 85% (видно оба изображения)
- ✅ Плавная анимация появления (0.3s)
- ✅ В консоли: `✅ Включен слой: ПЗУ`
- ✅ В консоли: `✅ Слой загружен: ПЗУ`

### 5. Повторно кликните на "ПЗУ"

**Ожидаемое поведение:**
- ✅ Кнопка возвращается в обычное состояние
- ✅ Слой плавно исчезает (0.3s)
- ✅ В консоли: `❌ Отключен слой: ПЗУ`

---

## 🎨 Визуальная структура кнопки ПЗУ

### Неактивное состояние:

```
┌──────────────────┐
│      ПЗУ         │
│                  │
│  bg: white       │
│  shadow: raised  │
│  color: #2C3E50  │
└──────────────────┘
```

### Активное состояние:

```
╔══════════════════╗
║      ПЗУ         ║
║                  ║
║  bg: #dde3f0     ║
║  shadow: inset   ║
║  color: #5F758D  ║
║  border: accent  ║
╚══════════════════╝
```

### Hover:

```
  ┌────────────────┐
 ││      ПЗУ      ││
 ││               ││
 ││  shadow: ↑   ││
  └────────────────┘
```

---

## 🔄 Наложение слоя

### Структура z-index:

```
z-index: 2  ← Слой ПЗУ (2.png)
              opacity: 0.85
              
z-index: 1  ← Основное изображение (основное.jpg)
              opacity: 1
```

### Позиционирование:

```jsx
position: 'absolute'
top: 0
left: 0
width: '100%'
height: '100%'
objectFit: 'contain'
```

**Результат:** Слой накладывается точно поверх генплана.

---

## 📊 Сравнение путей

| Слой | Кнопка | Путь к файлу |
|------|--------|--------------|
| Озеленение | Озеленение | `/genplan/Озеленение.png` |
| Детские площадки | Детские площадки | `/genplan/Детские площадки.png` |
| Освещение | Освещение | `/genplan/Освещение.png` |
| Парковки | Парковки | `/genplan/Парковки.png` |
| Пешеходные зоны | Пешеходные зоны | `/genplan/Пешеходные зоны.png` |
| Зоны отдыха | Зоны отдыха | `/genplan/Зоны отдыха.png` |
| **ПЗУ** | **ПЗУ** | **`/plans/genplan/2.png`** ✅ |

---

## 📁 Структура файлов (итоговая)

```
PUBLIC/
  ├── genplan/
  │   ├── основное.jpg      ✅ Существует
  │   └── 2.png             ✅ Существует
  │
  └── plans/
      └── genplan/
          └── 2.png         ⚠️ НУЖНО СКОПИРОВАТЬ ВРУЧНУЮ

dist/
  └── plans/
      └── genplan/
          └── 2.png         ⚠️ Создастся при build
```

---

## 🚀 Быстрая инструкция

### Шаг 1: Скопируйте файл

**Вручную через Explorer:**
1. Откройте `PUBLIC\genplan\`
2. Скопируйте `2.png`
3. Создайте папку `PUBLIC\plans\genplan\`
4. Вставьте `2.png`

### Шаг 2: Проверьте

Откройте страницу "Благоустройство" и:
1. Найдите кнопку "ПЗУ" внизу фильтров
2. Кликните на неё
3. Слой должен наложиться на генплан

### Шаг 3: Копия в dist (опционально)

При запуске `npm run build` файл автоматически скопируется.

Или вручную:
```powershell
Copy-Item ".\PUBLIC\plans\genplan\2.png" ".\dist\plans\genplan\2.png" -Force
```

---

## 📋 Чек-лист

- [x] Кнопка "ПЗУ" добавлена в код
- [x] Логика обработки пути настроена
- [x] Прозрачность 0.85
- [x] Анимация 0.3s ease-in-out
- [x] Файл существует: `PUBLIC/genplan/2.png`
- [ ] Файл скопирован в: `PUBLIC/plans/genplan/2.png` ← НУЖНО ВЫПОЛНИТЬ ВРУЧНУЮ
- [ ] Протестировано в браузере

---

## 🎯 Ожидаемый результат

**После копирования файла:**

1. На странице "Благоустройство" в панели фильтров появится кнопка **"ПЗУ"**
2. При клике на неё поверх генплана наложится цветной план благоустройства
3. Плавная анимация появления (0.3s)
4. Прозрачность 85% (видно оба слоя)
5. При повторном клике слой исчезает

**Все в едином неоморфном стиле!** 🎨✨

