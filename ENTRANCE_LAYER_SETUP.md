# Слой "Вход в здание"

## ✅ Настроено

### 🎯 Функциональность
При нажатии на кнопку **"Вход в здание"** поверх генплана накладывается слой с обозначением входных групп.

---

## 📁 Файловая структура

### Файлы существуют:

```
PUBLIC/plans/genplan/
  ├── основное.jpg  ✅ Основной генплан
  ├── 2.png         ✅ Слой ПЗУ
  └── 3.png         ✅ Слой "Вход в здание"
```

**Файл 3.png:**
- Формат: PNG с прозрачным фоном
- Содержание: Красные точки, обозначающие входы в здание
- Размер: Совпадает с основным изображением

---

## 🔧 Реализация в коде

### Логика определения пути:

**src/pages/Landscaping.jsx (строки 81-89):**

```jsx
{activeLayers.map((layer) => {
  // Для специальных слоёв используем особые пути
  let layerPath;
  if (layer === 'ПЗУ') {
    layerPath = '/plans/genplan/2.png';
  } else if (layer === 'Вход в здание') {
    layerPath = '/plans/genplan/3.png';  // ✅ Добавлено
  } else {
    layerPath = `/genplan/${layer}.png`;
  }
  
  return <motion.img src={layerPath} ... />
})}
```

### Таблица путей:

| Кнопка | Путь к файлу | Статус |
|--------|--------------|--------|
| Озеленение | `/genplan/Озеленение.png` | ⏳ |
| Детские площадки | `/genplan/Детские площадки.png` | ⏳ |
| Освещение | `/genplan/Освещение.png` | ⏳ |
| Парковки | `/genplan/Парковки.png` | ⏳ |
| Пешеходные зоны | `/genplan/Пешеходные зоны.png` | ⏳ |
| Зоны отдыха | `/genplan/Зоны отдыха.png` | ⏳ |
| **ПЗУ** | **`/plans/genplan/2.png`** | ✅ |
| Движение пешеходов | Компонент PedestrianLayer | ✅ |
| **Вход в здание** | **`/plans/genplan/3.png`** | ✅ |

---

## 🎨 Визуальный эффект

### При активации "Вход в здание":

```
┌────────────────────────────────┐
│  Генплан (основное.jpg)        │
│                                │
│  + Слой 3.png (красные точки)  │
│    ● Вход 1                    │
│    ● Вход 2                    │
│    ● Вход 3                    │
│    ● Входная группа            │
│                                │
│  Прозрачность: 85%             │
│  Анимация: fade-in 0.3s        │
└────────────────────────────────┘
```

### Наложение слоя:

```jsx
style={{
  position: 'absolute',
  top: 0,
  left: 0,
  width: '100%',
  height: '100%',
  objectFit: 'contain',
  zIndex: 2,
  pointerEvents: 'none',
  mixBlendMode: 'normal'
}}
```

**Характеристики:**
- Позиция: absolute (точно поверх генплана)
- Размер: 100% (полное покрытие)
- objectFit: contain (без искажений)
- Прозрачность: 85%
- z-index: 2 (поверх основного изображения)

---

## 🔄 Логика работы

### При клике на кнопку:

**1. Первый клик (включение):**
```
activeLayers: []
     ↓
toggleLayer('Вход в здание')
     ↓
activeLayers: ['Вход в здание']
     ↓
Слой отображается:
  - Fade-in анимация 0.3s
  - opacity: 0 → 0.85
     ↓
Консоль: ✅ Включен слой: Вход в здание
Консоль: ✅ Слой загружен: Вход в здание
```

**2. Повторный клик (выключение):**
```
activeLayers: ['Вход в здание']
     ↓
toggleLayer('Вход в здание')
     ↓
activeLayers: []
     ↓
Слой исчезает:
  - Fade-out анимация
  - opacity: 0.85 → 0
     ↓
Консоль: ❌ Отключен слой: Вход в здание
```

---

## 🎨 Состояния кнопки

### Неактивная:

```
┌─────────────────────┐
│  Вход в здание      │
│                     │
│ bg: white           │
│ shadow: raised      │
│ color: #2C3E50      │
└─────────────────────┘
```

### Активная:

```
╔═════════════════════╗
║  Вход в здание      ║
║                     ║
║ bg: #dde3f0         ║
║ shadow: inset       ║
║ color: #5F758D      ║
║ border: accent      ║
╚═════════════════════╝
```

### Hover:

```
  ┌───────────────────┐
 ││ Вход в здание    ││
 ││                  ││
 ││ shadow: enhanced ││
  └───────────────────┘
```

---

## 🧪 Тестирование

### 1. Откройте страницу "Благоустройство"

### 2. Найдите кнопку "Вход в здание"

Внизу панели фильтров (9-я кнопка):
```
☐ Озеленение
☐ Детские площадки
☐ Освещение
☐ Парковки
☐ Пешеходные зоны
☐ Зоны отдыха
☐ ПЗУ
☐ Движение пешеходов
☐ Вход в здание        ← ЗДЕСЬ
```

### 3. Кликните на "Вход в здание"

**Должно произойти:**
- ✅ Кнопка меняет стиль (активная)
- ✅ Поверх генплана накладывается слой `3.png`
- ✅ Видны **красные точки** (входные группы)
- ✅ Прозрачность 85% (видно оба изображения)
- ✅ Плавная анимация появления (0.3s)
- ✅ Консоль: `✅ Включен слой: Вход в здание`
- ✅ Консоль: `✅ Слой загружен: Вход в здание`

### 4. Повторно кликните

**Должно произойти:**
- ✅ Кнопка возвращается в обычное состояние
- ✅ Слой плавно исчезает
- ✅ Консоль: `❌ Отключен слой: Вход в здание`

### 5. Комбинация слоёв

Попробуйте активировать несколько слоёв одновременно:
```
☑ ПЗУ
☑ Вход в здание
☑ Движение пешеходов
```

**Результат:**
- Все слои накладываются друг на друга
- Видны все активные элементы
- Анимация пешеходов работает поверх всего

---

## 📊 Z-index иерархия

```
z-index: 9999  ← Движение пешеходов (SVG анимация)
                 PedestrianLayer
                 
z-index: 2     ← Статичные слои (PNG)
                 - ПЗУ (2.png)
                 - Вход в здание (3.png)
                 - Другие слои
                 
z-index: 1     ← Основное изображение
                 Генплан (основное.jpg)
```

---

## 🎨 Содержимое слоя 3.png

**Изображение показывает:**
- 🔴 Красные точки - входные группы в здание
- Расположение входов по периметру зданий
- Прозрачный фон (черные области = прозрачность)

**Назначение:**
- Визуализация точек входа
- Планирование навигации
- Показ главных и дополнительных входов

---

## 📁 Измененные файлы

**src/pages/Landscaping.jsx** (строки 82-89):

```diff
  {activeLayers.map((layer) => {
    let layerPath;
    if (layer === 'ПЗУ') {
      layerPath = '/plans/genplan/2.png';
+   } else if (layer === 'Вход в здание') {
+     layerPath = '/plans/genplan/3.png';
    } else {
      layerPath = `/genplan/${layer}.png`;
    }
```

---

## 🎯 Список всех фильтров (9)

| № | Кнопка | Тип | Путь | Статус |
|---|--------|-----|------|--------|
| 1 | Озеленение | PNG | `/genplan/Озеленение.png` | ⏳ |
| 2 | Детские площадки | PNG | `/genplan/Детские площадки.png` | ⏳ |
| 3 | Освещение | PNG | `/genplan/Освещение.png` | ⏳ |
| 4 | Парковки | PNG | `/genplan/Парковки.png` | ⏳ |
| 5 | Пешеходные зоны | PNG | `/genplan/Пешеходные зоны.png` | ⏳ |
| 6 | Зоны отдыха | PNG | `/genplan/Зоны отдыха.png` | ⏳ |
| 7 | ПЗУ | PNG | `/plans/genplan/2.png` | ✅ |
| 8 | Движение пешеходов | SVG | PedestrianLayer | ✅ |
| 9 | **Вход в здание** | **PNG** | **`/plans/genplan/3.png`** | **✅** |

---

## ✅ Готово!

**Кнопка "Вход в здание" полностью функциональна:**
- ✅ Файл существует: `PUBLIC/plans/genplan/3.png`
- ✅ Путь настроен: `/plans/genplan/3.png`
- ✅ Логика в коде: if/else для специальных путей
- ✅ Прозрачность: 85%
- ✅ Анимация: fade-in/out 0.3s
- ✅ Позиционирование: absolute, точное наложение
- ✅ Без ошибок линтера

**Кликните на "Вход в здание" и увидите красные точки входных групп!** 🚪✨

