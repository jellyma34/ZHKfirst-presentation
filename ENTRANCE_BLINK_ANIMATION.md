# Анимация пульсации "Вход в здание"

## ✅ Реализовано

### 🎯 Функциональность
При нажатии на кнопку **"Вход в здание"** слой с входными группами плавно появляется и **пульсирует**, создавая эффект выделения точек входа.

---

## 🎨 Анимационные эффекты

### 1. Fade-in (появление):

```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

Duration: 0.3s
Easing: ease-in-out
```

### 2. Blink (пульсация):

```css
@keyframes blink {
  0%, 100% { 
    opacity: 1;           /* Полная видимость */
    transform: scale(1);  /* Обычный размер */
  }
  50% { 
    opacity: 0.6;         /* 60% прозрачности */
    transform: scale(1.01); /* Легкое увеличение */
  }
}

Duration: 1.5s
Repeat: infinite
Easing: ease-in-out
```

### 3. Дополнительные эффекты:

**mix-blend-mode: screen**
- Создает эффект подсветки
- Красные точки светятся ярче

**filter: drop-shadow(0 0 6px rgba(255, 255, 255, 0.4))**
- Мягкое свечение вокруг точек входа
- Усиливает эффект выделения

**z-index: 50**
- Поверх обычных слоёв (z-index: 2)
- Ниже анимации пешеходов (z-index: 9999)

---

## 🔄 Цикл анимации

### Временная шкала (1.5 секунды):

```
0.0s   ━━━━━━━━━  100% opacity, scale(1.0)
0.2s   ╲          
0.4s    ╲         
0.6s     ╲        
0.75s    ━━━━━━━  60% opacity, scale(1.01)  ← Минимум
0.9s     ╱        
1.1s    ╱         
1.3s   ╱          
1.5s   ━━━━━━━━━  100% opacity, scale(1.0)  ← Цикл повторяется
```

### График opacity:

```
1.0  ┃━╲     ╱━╲     ╱━
0.9  ┃  ╲   ╱   ╲   ╱
0.8  ┃   ╲ ╱     ╲ ╱
0.7  ┃    ╲╱       ╲╱
0.6  ┃    ━        ━
     ┗━━━━━━━━━━━━━━━━━
     0s  0.75s  1.5s  2.25s
```

---

## 🎨 Визуальный эффект

### При активации "Вход в здание":

```
Кадр 1 (0s):
┌──────────────────────┐
│ Генплан              │
│  🔴🔴🔴 (100%)       │ ← Красные точки яркие
│  Входы видны четко   │
└──────────────────────┘

Кадр 2 (0.75s):
┌──────────────────────┐
│ Генплан              │
│  ⭕⭕⭕ (60%)        │ ← Точки тускнеют
│  Входы приглушены    │
└──────────────────────┘

Кадр 3 (1.5s):
┌──────────────────────┐
│ Генплан              │
│  🔴🔴🔴 (100%)       │ ← Снова яркие
│  Цикл повторяется    │
└──────────────────────┘
```

### Эффекты:

```
Opacity:  100% → 60% → 100% (циклично)
Scale:    1.0  → 1.01 → 1.0  (легкое дыхание)
Shadow:   Мягкое свечение (постоянно)
Blend:    screen (подсветка)
```

---

## 🔧 Реализация в коде

### JSX (Landscaping.jsx):

**Условие для className:**
```jsx
className={layer === 'Вход в здание' ? 'entrance-blink' : ''}
```

**Z-index:**
```jsx
zIndex: layer === 'Вход в здание' ? 50 : 2
```

**Mix-blend-mode:**
```jsx
mixBlendMode: layer === 'Вход в здание' ? 'screen' : 'normal'
```

**Filter:**
```jsx
filter: layer === 'Вход в здание' ? 'drop-shadow(0 0 6px rgba(255, 255, 255, 0.4))' : 'none'
```

**Opacity:**
```jsx
animate={{ opacity: layer === 'Вход в здание' ? 1 : 0.85 }}
```

### CSS (landscaping.css):

**Класс анимации:**
```css
.entrance-blink {
  animation: 
    fadeIn 0.3s ease-in-out forwards,
    blink 1.5s ease-in-out infinite !important;
}
```

**Две анимации:**
1. `fadeIn` - однократно при появлении (0.3s)
2. `blink` - бесконечный цикл пульсации (1.5s)

---

## 📊 Параметры анимации

| Параметр | Значение | Назначение |
|----------|----------|------------|
| **fade-in duration** | 0.3s | Появление слоя |
| **blink duration** | 1.5s | Цикл пульсации |
| **opacity min** | 60% | Минимальная яркость |
| **opacity max** | 100% | Максимальная яркость |
| **scale min** | 1.0 | Обычный размер |
| **scale max** | 1.01 | Легкое увеличение |
| **z-index** | 50 | Поверх обычных слоёв |
| **mix-blend-mode** | screen | Эффект подсветки |
| **drop-shadow** | 6px white | Свечение |

---

## 📁 Z-index иерархия

```
z-index: 9999  ← Движение пешеходов (SVG)
                 PedestrianLayer
                 Анимация поверх всего
                 
z-index: 50    ← Вход в здание (PNG)
                 Пульсирующий слой
                 С эффектом screen
                 
z-index: 2     ← Остальные слои (PNG)
                 - ПЗУ
                 - Озеленение
                 - и т.д.
                 
z-index: 1     ← Основное изображение
                 Генплан
```

---

## 🧪 Как проверить

### 1. Откройте страницу "Благоустройство"

### 2. Найдите кнопку "Вход в здание"

Последняя кнопка в панели фильтров.

### 3. Кликните на "Вход в здание"

**Должно произойти:**
- ✅ Кнопка активируется (вдавленная, accent)
- ✅ Слой **плавно появляется** (fade-in 0.3s)
- ✅ Видны **красные точки входных групп**
- ✅ Точки начинают **пульсировать** (1.5s цикл)
- ✅ **Эффекты:**
  - Opacity меняется: 100% → 60% → 100%
  - Легкое увеличение: scale(1.0) → scale(1.01)
  - Мягкое свечение вокруг точек
  - Режим наложения: screen (подсветка)
- ✅ Консоль: `✅ Включен слой: Вход в здание`
- ✅ Консоль: `✅ Слой загружен: Вход в здание`

### 4. Наблюдайте пульсацию

**Эффект "дыхания":**
- Красные точки плавно светлеют и темнеют
- Легкое увеличение/уменьшение размера
- Мягкое свечение создает акцент
- Бесконечный цикл 1.5 секунды

### 5. Повторно кликните

**Должно произойти:**
- ✅ Кнопка деактивируется
- ✅ Слой плавно исчезает (fade-out)
- ✅ Пульсация останавливается
- ✅ Консоль: `❌ Отключен слой: Вход в здание`

### 6. Комбинация слоёв

Попробуйте активировать:
```
☑ ПЗУ (план благоустройства)
☑ Вход в здание (пульсирующие точки)
☑ Движение пешеходов (анимация)
```

**Иерархия отображения:**
- Движение пешеходов (поверх всего)
- Вход в здание (пульсирует)
- ПЗУ (статичный)
- Генплан (основа)

---

## 🎬 Эффекты в деталях

### mix-blend-mode: screen

```
Обычный режим:  Красный + Фото = Красный закрывает фото
Screen режим:   Красный + Фото = Красный подсвечивает фото
                                 (светлее, ярче)
```

**Результат:** Точки входа светятся, а не закрывают генплан.

### drop-shadow filter

```css
filter: drop-shadow(0 0 6px rgba(255, 255, 255, 0.4))
```

**Эффект:** Мягкое белое свечение вокруг красных точек (радиус 6px).

### transform: scale(1.01)

**Эффект:** Легкое "дыхание" - точки чуть увеличиваются в момент максимальной прозрачности.

---

## 📊 Сравнение с другими слоями

| Слой | z-index | opacity | Анимация | Эффекты |
|------|---------|---------|----------|---------|
| ПЗУ | 2 | 0.85 | fade-in | - |
| Озеленение | 2 | 0.85 | fade-in | - |
| **Вход в здание** | **50** | **1→0.6→1** | **fade-in + blink** | **screen + shadow** |
| Движение пешеходов | 9999 | 0.9 | requestAnimationFrame | glow |

---

## 📁 Измененные файлы

### 1. src/pages/Landscaping.jsx

**Строка 96:**
```jsx
+ className={layer === 'Вход в здание' ? 'entrance-blink' : ''}
```

**Строки 104-107:**
```jsx
+ zIndex: layer === 'Вход в здание' ? 50 : 2,
+ mixBlendMode: layer === 'Вход в здание' ? 'screen' : 'normal',
+ filter: layer === 'Вход в здание' ? 'drop-shadow(0 0 6px rgba(255, 255, 255, 0.4))' : 'none'
```

**Строка 110:**
```jsx
+ animate={{ opacity: layer === 'Вход в здание' ? 1 : 0.85 }}
```

### 2. src/styles/landscaping.css

**Добавлено (строки 17-40):**
```css
+ .entrance-blink {
+   animation: fadeIn 0.3s ease-in-out forwards, 
+              blink 1.5s ease-in-out infinite !important;
+ }
+ 
+ @keyframes fadeIn { ... }
+ @keyframes blink { ... }
```

---

## ✅ Результат

**Кнопка "Вход в здание" теперь включает:**
- ✨ Плавное появление (0.3s)
- ✨ Непрерывная пульсация (1.5s цикл)
- ✨ Изменение opacity: 100% → 60% → 100%
- ✨ Легкое "дыхание": scale(1.0) → scale(1.01)
- ✨ Эффект подсветки (mix-blend-mode: screen)
- ✨ Мягкое свечение (drop-shadow)
- ✨ z-index: 50 (выше обычных слоёв)
- ✨ Плавное исчезновение при отключении

**Привлекает внимание к входным группам!** 🚪💫✨

