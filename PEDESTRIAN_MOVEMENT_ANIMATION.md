# Анимация движения пешеходов

## ✅ Реализовано

### 🎯 Функциональность
При нажатии на кнопку **"Движение пешеходов"** поверх генплана появляется анимированный SVG-слой с движущимися пешеходами по заранее заданным маршрутам.

---

## 📁 Созданные файлы

### 1. Компонент анимации

**src/components/PedestrianLayer.jsx** - новый компонент

**Особенности:**
- ✅ SVG слой с 5 маршрутами
- ✅ 10-15 анимированных пешеходов
- ✅ Движение через requestAnimationFrame
- ✅ Пульсация пешеходов
- ✅ Тени под пешеходами
- ✅ Автоматическая очистка при размонтировании
- ✅ z-index: 9999 (поверх всех слоёв)

### 2. Интеграция

**src/pages/Landscaping.jsx** - обновлен

**Изменения:**
- Импорт PedestrianLayer
- Кнопка "Движение пешеходов" (было "Пути пешеходов")
- Условный рендеринг анимации
- Старый PedestrianAnimation.jsx удален

---

## 🎨 Маршруты пешеходов

### 5 анимированных маршрутов:

| № | Название | Цвет | Path | Пешеходов |
|---|----------|------|------|-----------|
| 1 | Главный вход | 🔵 #3B82F6 | Вход → Здание | 2-4 |
| 2 | От парковки | 🟢 #10B981 | Парковка → Вход | 2-4 |
| 3 | От площадки | 🟠 #F59E0B | Площадка → Здание | 2-4 |
| 4 | Периметр | 🟣 #8B5CF6 | По периметру | 2-4 |
| 5 | Поперечный | 🌸 #EC4899 | Через территорию | 2-4 |

**Всего:** 10-15 пешеходов на всех маршрутах

---

## 🔄 Как работает анимация

### requestAnimationFrame:

```javascript
const animate = () => {
  const deltaTime = currentTime - lastTime;
  
  pedestrians.forEach(ped => {
    // Обновляем прогресс
    ped.progress += ped.speed * deltaTime;
    
    // Зацикливаем
    if (ped.progress > 1) ped.progress = 0;
    
    // Получаем позицию
    const point = path.getPointAtLength(progress * pathLength);
    
    // Обновляем SVG элемент
    element.setAttribute('cx', point.x);
    element.setAttribute('cy', point.y);
  });
  
  requestAnimationFrame(animate);
};
```

### Параметры пешеходов:

```javascript
progress: случайная начальная позиция (0-1)
speed: 0.00015 - 0.00025 (разная скорость)
size: 6-9px (разный размер)
color: цвет маршрута
```

### Cleanup:

```javascript
useEffect(() => {
  // ...анимация
  
  return () => {
    cancelAnimationFrame(animationFrameRef.current);
  };
}, []);
```

---

## 🎨 Визуальная структура

### SVG слой:

```
┌────────────────────────────────────┐
│  z-index: 9999 (поверх всего)     │
│  opacity: 1 (fade-in 0.4s)        │
│                                    │
│  ┌──────→ маршрут 1 (синий)       │
│  │  ●──→ пешеходы                 │
│  └→                                │
│                                    │
│  ┌───→ маршрут 2 (зеленый)        │
│  │ ●→                              │
│  └→                                │
│                                    │
│  ... еще 3 маршрута                │
│                                    │
│  Эффекты:                          │
│  - Пульсация размера               │
│  - Пульсация прозрачности         │
│  - Тени под пешеходами            │
│  - Свечение (filter: glow)        │
└────────────────────────────────────┘
```

### Слои (z-index иерархия):

```
z-index: 9999  ← Анимация пешеходов (PedestrianLayer)
                 SVG с движением
                 
z-index: 2     ← Другие слои (PNG)
                 Статичные изображения
                 
z-index: 1     ← Основное изображение
                 Генплан
```

---

## 🎮 Управление

### Кнопка в панели фильтров:

```
╔════════════════════════╗
║ Фильтры слоёв          ║
║                        ║
║ □ Озеленение           ║
║ □ Детские площадки     ║
║ □ Освещение            ║
║ □ Парковки             ║
║ □ Пешеходные зоны      ║
║ □ Зоны отдыха          ║
║ □ ПЗУ                  ║
║ □ Движение пешеходов   ║ ← АНИМАЦИЯ
║ □ Вход в здание        ║
╚════════════════════════╝
```

### Логика:

**При клике:**
```
activeLayers: [] 
     ↓ toggleLayer('Движение пешеходов')
activeLayers: ['Движение пешеходов']
     ↓
PedestrianLayer рендерится
     ↓
Анимация запускается
```

**При повторном клике:**
```
activeLayers: ['Движение пешеходов']
     ↓ toggleLayer('Движение пешеходов')
activeLayers: []
     ↓
PedestrianLayer размонтируется
     ↓
cancelAnimationFrame вызывается
     ↓
Анимация останавливается
```

---

## 🧪 Как проверить

### 1. Откройте страницу "Благоустройство"

### 2. Найдите кнопку "Движение пешеходов"

В панели фильтров (8-я кнопка в списке).

### 3. Кликните на неё

**Должно произойти:**
- ✅ Кнопка меняет стиль (активное состояние)
- ✅ Появляется SVG слой (fade-in 0.4s)
- ✅ Видны 5 пунктирных путей разных цветов
- ✅ По путям движутся цветные точки (пешеходы)
- ✅ Пешеходы пульсируют (размер и прозрачность)
- ✅ Под каждым пешеходом тень
- ✅ Плавное движение ~60fps
- ✅ Консоль: `✅ Включен слой: Движение пешеходов`

### 4. Наблюдайте анимацию

**Эффекты:**
- Пешеходы движутся по кривым маршрутам (Bezier curves)
- Разная скорость для каждого
- Пульсация: r увеличивается/уменьшается
- Opacity меняется: 0.7 → 0.95 → 0.7
- Тени следуют за пешеходами
- Свечение вокруг пешеходов (filter: glow)

### 5. Повторно кликните

**Должно произойти:**
- ✅ Кнопка возвращается в обычное состояние
- ✅ SVG слой исчезает (fade-out)
- ✅ Анимация останавливается
- ✅ requestAnimationFrame очищается
- ✅ Консоль: `❌ Отключен слой: Движение пешеходов`

---

## 💡 Технические детали

### SVG Paths (Bezier curves):

```javascript
// Кривые маршруты через Q (quadratic Bezier)
'M 100 800 Q 300 700 500 600 T 900 450'
   ↑       ↑         ↑         ↑
  Start  Control   Point    End (smooth)
```

### Движение по пути:

```javascript
pathElement.getTotalLength()           // Длина пути
pathElement.getPointAtLength(distance) // Координаты на пути

progress: 0 → 1 (0% → 100% пути)
distance: progress × pathLength
point: {x, y} координаты на дистанции
```

### Пульсация (SVG animate):

```xml
<animate
  attributeName="r"
  values="6;8;6"        <!-- Размер пульсирует -->
  dur="1.5s"            <!-- Длительность -->
  repeatCount="indefinite"
/>
```

### Фильтры SVG:

```xml
<filter id="glow">
  <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
  <feMerge>
    <feMergeNode in="coloredBlur"/>
    <feMergeNode in="SourceGraphic"/>
  </feMerge>
</filter>
```

Создает мягкое свечение вокруг пешеходов.

---

## 📊 Параметры анимации

| Параметр | Значение | Описание |
|----------|----------|----------|
| **Маршрутов** | 5 | Разные пути |
| **Пешеходов** | 10-15 | 2-4 на маршрут |
| **Скорость** | 0.00015-0.00025 | Разная для каждого |
| **Размер** | 6-9px | Случайный |
| **FPS** | ~60 | requestAnimationFrame |
| **Прозрачность** | 0.7-0.95 | Пульсация |
| **z-index** | 9999 | Поверх всего |
| **Fade-in** | 0.4s | Плавное появление |

---

## 🔧 Кастомизация

### Добавить новый маршрут:

```javascript
// В PedestrianLayer.jsx
const routes = [
  // ...существующие
  {
    id: 'route6',
    path: 'M startX startY Q controlX controlY endX endY',
    color: '#FF0000',
    name: 'Новый маршрут'
  }
];
```

### Изменить скорость движения:

```javascript
speed: 0.0003 + Math.random() * 0.0002  // Быстрее
```

### Изменить количество пешеходов:

```javascript
const count = 3 + Math.floor(Math.random() * 3);  // 3-6 на маршрут
```

### Настроить пути под ваш генплан:

1. Откройте изображение генплана
2. Определите ключевые точки (вход, здание, парковка)
3. Запишите координаты
4. Обновите SVG paths в коде

---

## ⚡ Производительность

### Оптимизации:

- ✅ requestAnimationFrame (синхронизация с экраном)
- ✅ deltaTime для стабильной скорости
- ✅ pointer-events: none (не блокирует UI)
- ✅ Cleanup при размонтировании
- ✅ Минимальные DOM манипуляции

### Результат:

- Плавная анимация ~60fps
- Нет утечек памяти
- Автоматическая остановка при выключении
- Низкая нагрузка на CPU

---

## 📁 Измененные/созданные файлы

1. ✅ **`src/components/PedestrianLayer.jsx`** - создан (215 строк)
   - SVG анимация
   - 5 маршрутов
   - requestAnimationFrame
   - useEffect cleanup
   - Пульсация и свечение

2. ✅ **`src/pages/Landscaping.jsx`** - обновлен
   - Импорт PedestrianLayer
   - Кнопка "Движение пешеходов"
   - Условный рендеринг

3. ✅ **`src/components/PedestrianAnimation.jsx`** - удален (заменен на PedestrianLayer)

---

## 🎯 Результат

**Кнопка "Движение пешеходов" теперь:**
- ✅ Активирует анимированный SVG слой
- ✅ 5 цветных маршрутов
- ✅ 10-15 движущихся пешеходов
- ✅ Пульсация и тени
- ✅ Свечение вокруг пешеходов
- ✅ Плавное появление/исчезновение
- ✅ Автоматическая очистка (no memory leaks)
- ✅ Производительная анимация (60fps)
- ✅ z-index: 9999 (поверх всех слоёв)

**Живая анимация движения на генплане!** 🚶‍♂️🚶‍♀️✨

