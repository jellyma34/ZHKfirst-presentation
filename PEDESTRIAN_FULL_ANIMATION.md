# Полноценная анимация движения пешеходов

## ✅ Реализовано

### 🎯 Функциональность
При включении фильтра **"Движение пешеходов"** на генплане появляется SVG-контейнер с **12 анимированными пешеходами**, движущимися по **5 маршрутам** с разной скоростью и направлением.

---

## 🚶 Логика движения

### Алгоритм из HTML-примера:

```javascript
// 1. Создание пешехода на случайном маршруте
const pathEl = choice(routes);              // Случайный маршрут
const reverse = Math.random() < 0.5;        // Случайное направление
const color = hsl(random, 80%, 55%);        // Случайный цвет

// 2. Движение по пути
t += speed * deltaTime * direction;         // Прогресс по пути

// 3. Проверка границ
if (t > totalLength || t < 0) {
  if (Math.random() < 0.6) {
    direction *= -1;                        // 60% - разворот
  } else {
    t = direction > 0 ? 0 : totalLength;   // 40% - сначала
  }
}

// 4. Получение позиции
const point = path.getPointAtLength(t);
element.setAttribute('transform', `translate(${point.x} ${point.y})`);
```

---

## 📐 Маршруты (5 путей)

### SVG paths:

```jsx
1. route-a: Горизонтальный верхний
   M 120 170 C 250 170 360 170 510 170 C 660 170 900 170 1060 170
   
2. route-b: Диагональный снизу
   M 90 600 C 240 520 380 450 520 430 C 720 400 880 430 1080 360
   
3. route-c: Средний волнообразный
   M 170 350 C 300 350 320 510 520 520 C 700 530 880 520 990 520
   
4. route-d: Средний-верхний
   M 100 450 C 250 420 400 400 550 380 C 700 360 850 340 1000 320
   
5. route-e: Верхний волнообразный
   M 200 250 C 350 280 500 300 650 290 C 800 280 950 260 1100 240
```

**Визуальная карта:**
```
      route-a ─────────────────────────
      route-e ～～～～～～～～～～～～
      route-d ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
      route-c ～～～～～～～～～～～～
      route-b ╲  ╲  ╲  ╲  ╲  ╲  ╲  ╲
```

---

## 👥 Пешеходы (12 персон)

### Параметры каждого пешехода:

```javascript
count: 12                          // Всего пешеходов
personRadius: 5px                  // Размер кружка
minSpeed: 40                       // Минимальная скорость
maxSpeed: 80                       // Максимальная скорость
jitter: ±3px                       // Случайное отклонение
color: hsl(random, 80%, 55%)       // Случайный цвет
reverse: 50% шанс                  // Направление движения
spawnDelay: 0-2000ms               // Задержка появления
```

### Поведение:

**При достижении конца пути:**
- 60% шанс - разворот (dir *= -1)
- 40% шанс - телепорт в начало (t = 0)

**Эффекты:**
- Пульсация размера (scale 1.0 → 0.85)
- Пульсация прозрачности (opacity 1.0 → 0.6)
- Мягкое свечение (drop-shadow)
- Случайное отклонение (jitter)

---

## 🎨 Визуальная структура

### SVG слой:

```
┌────────────────────────────────────┐
│  viewBox="0 0 1200 675"            │
│  z-index: 130                      │
│                                    │
│  Маршруты (пунктирные линии):     │
│  ═══════════════════════════       │
│  ～～～～～～～～～～～～～        │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─          │
│                                    │
│  Пешеходы (цветные кружки):       │
│  ● ● ●  ← Движутся по путям       │
│    ●      Разная скорость          │
│      ●    Разные цвета             │
│        ●  Пульсируют               │
└────────────────────────────────────┘
```

### Анимации:

**1. Маршруты (route):**
```css
stroke-dasharray: 5 8              /* Пунктир */
animation: routeDash 4s infinite   /* Движение пунктира */
```

**2. Пешеходы (person .body):**
```css
animation: walkPulse 1.2s infinite  /* Пульсация */
filter: drop-shadow(...)            /* Мягкое свечение */
```

---

## ⚡ Производительность

### Оптимизации:

```javascript
requestAnimationFrame(loop)       // ~60fps
deltaTime = min(0.05, realDelta)  // Ограничение скачков
getPointAtLength(distance)        // Нативный SVG API
setAttribute('transform', ...)    // Прямые DOM манипуляции
```

### Cleanup:

```javascript
useEffect(() => {
  // ...анимация
  
  return () => {
    cancelAnimationFrame(animationFrameRef.current);
    peopleLayer.innerHTML = '';
    walkersRef.current = [];
  };
}, []);
```

**Результат:**
- Плавная анимация ~60fps
- Нет утечек памяти
- Автоматическая остановка

---

## 🧪 Как проверить

### 1. Откройте страницу "Благоустройство"

### 2. Кликните на "Движение пешеходов"

**Должно произойти:**
- ✅ SVG слой плавно появляется (fade-in 0.4s)
- ✅ Видны **5 пунктирных маршрутов** (голубые)
- ✅ Пунктир **движется** (анимация dash)
- ✅ По маршрутам движутся **12 цветных кружков**
- ✅ Каждый пешеход:
  - Случайный цвет (HSL)
  - Случайная скорость (40-80)
  - Случайное направление
  - Пульсация размера и прозрачности
  - Мягкое свечение
- ✅ Пешеходы **разворачиваются** или **телепортируются**
- ✅ Появляются с задержкой (0-2s)
- ✅ Плавное движение ~60fps

### 3. Наблюдайте движение

**Эффекты:**
- Пешеходы идут в разных направлениях
- При достижении конца пути - разворот или телепорт
- Пульсация: size 1.0 → 0.85
- Прозрачность: 1.0 → 0.6
- Пунктир маршрута движется (эффект потока)

### 4. Повторно кликните

**Должно произойти:**
- ✅ SVG слой исчезает
- ✅ Анимация останавливается
- ✅ requestAnimationFrame очищается
- ✅ Все пешеходы удаляются

---

## 📊 Параметры конфигурации

| Параметр | Значение | Назначение |
|----------|----------|------------|
| **count** | 12 | Количество пешеходов |
| **speedK** | 1.0 | Множитель скорости |
| **personRadius** | 5px | Размер кружка |
| **jitter** | ±3px | Случайное отклонение |
| **minSpeed** | 40 | Минимальная скорость |
| **maxSpeed** | 80 | Максимальная скорость |
| **spawnDelayMax** | 2000ms | Задержка появления |
| **viewBox** | 0 0 1200 675 | Координатное пространство |
| **z-index** | 130 | Поверх обычных слоёв |

---

## 🎨 Стили CSS

### Маршруты:

```css
.route {
  stroke: rgba(150, 180, 255, 0.4);  /* Голубой пунктир */
  stroke-width: 2px;
  stroke-dasharray: 5 8;             /* 5px линия, 8px пробел */
  animation: routeDash 4s infinite;  /* Движение пунктира */
}
```

### Пешеходы:

```css
.person .body {
  fill: #ffd166;                     /* Желтый (по умолчанию) */
  animation: walkPulse 1.2s infinite; /* Пульсация */
  filter: drop-shadow(0 0 3px rgba(255, 255, 255, 0.8)); /* Свечение */
}

@keyframes walkPulse {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(0.85); opacity: 0.6; }
}
```

---

## 🔄 Z-index иерархия

```
z-index: 130   ← Движение пешеходов (PedestrianLayer)
                 SVG с ходьбой по маршрутам
                 
z-index: 100   ← Вход в здание
                 Пульсирующие точки
                 
z-index: 2     ← Обычные слои (PNG)
                 Статичные изображения
                 
z-index: 1     ← Основное изображение
                 Генплан
```

---

## 📁 Созданные/обновленные файлы

1. ✅ **`src/components/PedestrianLayer.jsx`** - полностью переписан
   - Логика движения из HTML-примера
   - 5 маршрутов
   - 12 пешеходов
   - requestAnimationFrame
   - Разворот/телепорт логика
   - Cleanup

2. ✅ **`src/styles/landscaping.css`** - добавлены стили
   - .pedestrian-overlay
   - .route с анимацией dash
   - .person .body с пульсацией
   - @keyframes routeDash
   - @keyframes walkPulse

3. ✅ **`src/pages/Landscaping.jsx`** - уже настроен
   - Импорт PedestrianLayer
   - Условный рендеринг

**Линтер:** ✅ Нет ошибок

---

## 🎉 Готово!

**Полноценная анимация движения пешеходов:**
- ✅ 12 пешеходов на 5 маршрутах
- ✅ Логика из HTML-примера
- ✅ Разная скорость (40-80)
- ✅ Случайные цвета (HSL)
- ✅ Разворот или телепорт
- ✅ Пульсация размера и прозрачности
- ✅ Движущийся пунктир маршрутов
- ✅ requestAnimationFrame (~60fps)
- ✅ Автоматическая очистка
- ✅ z-index: 130

**Кликните на "Движение пешеходов" и наблюдайте живую анимацию!** 🚶‍♂️🚶‍♀️✨

