# 🎠 Навигация carousel на главной странице

## ✅ Реализовано

Добавлена полнофункциональная система навигации с кнопками влево/вправо и индикаторами для карточек на главной странице.

---

## 🎯 Что добавлено:

### 1. **Кнопки навигации** ⬅️ ➡️

```jsx
<motion.button
  onClick={prevCard}
  className="carousel-nav-button left">
  <ChevronLeft size={28} />
</motion.button>

<motion.button
  onClick={nextCard}
  className="carousel-nav-button right">
  <ChevronRight size={28} />
</motion.button>
```

**Стили:**
```css
.carousel-nav-button {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(8px);
  box-shadow: 6px 6px 12px rgba(0, 0, 0, 0.1), 
              -6px -6px 12px rgba(255, 255, 255, 0.9);
}
```

---

### 2. **Логика переключения** 🔄

```javascript
const prevCard = () => {
  setCurrentIndex((prev) => 
    prev === 0 ? interactiveSlides.length - 1 : prev - 1
  );
};

const nextCard = () => {
  setCurrentIndex((prev) => 
    prev === interactiveSlides.length - 1 ? 0 : prev + 1
  );
};
```

**Поведение:**
- Циклическое переключение
- ⬅️ Влево: 4→3→2→1→0→4...
- ➡️ Вправо: 0→1→2→3→4→0...

---

### 3. **Плавная анимация смены** 🌊

```jsx
<AnimatePresence mode="wait">
  <motion.div
    key={currentIndex}
    initial={{ opacity: 0, x: 100, scale: 0.9 }}
    animate={{ opacity: 1, x: 0, scale: 1 }}
    exit={{ opacity: 0, x: -100, scale: 0.9 }}
    transition={{ 
      duration: 0.6,
      ease: [0.4, 0, 0.2, 1]
    }}
  >
    {/* Карточка */}
  </motion.div>
</AnimatePresence>
```

**Эффект:**
- **Initial**: Появляется справа (x: 100), уменьшенная (scale: 0.9)
- **Animate**: Плавно входит в центр (x: 0), нормальный размер (scale: 1)
- **Exit**: Уходит влево (x: -100), уменьшается (scale: 0.9)
- **Длительность**: 600ms
- **Easing**: Cubic-bezier для плавности

---

### 4. **Индикаторы карточек** ⚫⚫⚫⚫⚫

```jsx
<div className="carousel-indicators">
  {interactiveSlides.map((_, idx) => (
    <button
      key={idx}
      onClick={() => setCurrentIndex(idx)}
      className={`indicator-dot ${idx === currentIndex ? 'active' : ''}`}
    />
  ))}
</div>
```

**Стили:**
```css
.indicator-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: rgba(200, 169, 106, 0.3);
}

.indicator-dot.active {
  width: 32px;                    /* Растягивается */
  border-radius: 6px;
  background: #C8A96A;            /* Золотой акцент */
  box-shadow: 0 0 12px rgba(200, 169, 106, 0.6);
}
```

---

### 5. **Автоматическая смена** ⏱️

```javascript
useEffect(() => {
  const timer = setInterval(() => {
    nextCard();
  }, 5000); // 5 секунд

  return () => clearInterval(timer);
}, [currentIndex]);
```

**Поведение:**
- Автоматическая смена каждые **5 секунд**
- Останавливается при ручном переключении
- Возобновляется после паузы

---

## 📐 Структура carousel:

```
┌────────────────────────────────────────────────┐
│                                                │
│   ⬅️           [Карточка]            ➡️      │
│              (600×600px)                       │
│                                                │
│              ⚫⚫⚪⚫⚫                           │
│           (индикаторы)                         │
└────────────────────────────────────────────────┘
```

---

## 🎨 Визуальные эффекты:

### Кнопки навигации:

| Состояние | Эффект |
|-----------|--------|
| **Normal** | Полупрозрачный белый фон, неоморфные тени |
| **Hover** | scale(1.1), увеличение теней |
| **Active** | scale(0.95), вдавленные тени |

### Индикаторы:

| Состояние | Размер | Цвет | Эффект |
|-----------|--------|------|--------|
| **Inactive** | 12×12px | Полупрозрачный золотой | Круглый |
| **Active** | 32×12px | Яркое золото | Растянутый, свечение |
| **Hover** | scale(1.2) | Более яркий | - |

### Анимация карточек:

```
Предыдущая карточка → Fade-out влево (600ms)
                    ↓
        Новая карточка → Fade-in справа (600ms)
```

---

## 🎮 Управление:

### Способы переключения:

1. **Кнопки влево/вправо** - клик
2. **Индикаторы** - клик по точке
3. **Автоматическая смена** - каждые 5 секунд

### Клавиатурная навигация (можно добавить):
```javascript
useEffect(() => {
  const handleKeyPress = (e) => {
    if (e.key === 'ArrowLeft') prevCard();
    if (e.key === 'ArrowRight') nextCard();
  };
  
  window.addEventListener('keydown', handleKeyPress);
  return () => window.removeEventListener('keydown', handleKeyPress);
}, []);
```

---

## 📱 Адаптивность:

### Desktop (>1024px):
- Размер карточки: 600×600px
- Кнопки: 56×56px, по краям
- Индикаторы: 12px, снизу

### Tablet (768-1024px):
- Размер карточки: 600×600px
- Кнопки: 48×48px, ближе к центру
- Индикаторы: 12px

### Mobile (<768px):
- Размер карточки: 100% × 500px
- Кнопки: 44×44px, на краях
- Индикаторы: 10px, компактные

---

## 📊 Технические детали:

### Состояние:
```javascript
const [currentIndex, setCurrentIndex] = useState(0);
```

### Массив карточек:
```javascript
const interactiveSlides = [...]; // 5 карточек
```

### Навигация:
```javascript
// Циклическая навигация
const prevCard = () => setCurrentIndex((prev) => 
  prev === 0 ? cards.length - 1 : prev - 1
);
```

### AnimatePresence:
```jsx
<AnimatePresence mode="wait">
  {/* Только одна карточка отображается */}
</AnimatePresence>
```

**mode="wait"** гарантирует, что предыдущая карточка полностью исчезнет перед появлением новой.

---

## 📁 Измененные файлы:

1. ✅ **`src/pages/HomePage.jsx`**
   - Добавлен import `AnimatePresence`, `ChevronLeft`, `ChevronRight`
   - Добавлено состояние `currentIndex`
   - Функции `prevCard()` и `nextCard()`
   - Автоматическая смена каждые 5 секунд
   - Кнопки навигации с Framer Motion
   - Индикаторы с кликами

2. ✅ **`src/index.css`**
   - `.cards-carousel-wrapper` - контейнер
   - `.cards-carousel-container` - область карточек
   - `.carousel-nav-button` - стили кнопок
   - `.carousel-indicators` - индикаторы
   - `.indicator-dot` - точки индикаторов
   - Адаптивные media queries

3. ✅ **`HOMEPAGE_CAROUSEL_NAVIGATION.md`** - Эта документация

---

## ✨ Визуальные особенности:

### Кнопки:
- 🔘 Круглые (56px)
- 🌫️ Стеклянный эффект (backdrop-filter: blur)
- 🪞 Неоморфные тени
- 💫 Hover анимация (scale 1.1)
- 👆 Active состояние (вдавленные тени)

### Индикаторы:
- ⚫ Круглые точки (12px)
- 🟡 Золотые (#C8A96A)
- 📏 Активный растягивается (32px)
- ✨ Свечение активного индикатора
- 🎯 Кликабельные для прямого перехода

### Анимации:
- ⏱️ 600ms transitions
- 🎭 Cubic-bezier easing
- 🔄 Slide эффект (x: 100 → 0 → -100)
- 📏 Scale эффект (0.9 → 1 → 0.9)
- 🌊 Плавная смена без рывков

---

## 🎯 Результат:

**Было:**
- Все 5 карточек одновременно
- Горизонтальная прокрутка
- Hover для увеличения

**Стало:**
- ✅ **Одна карточка** в центре внимания
- ✅ **Кнопки навигации** влево/вправо
- ✅ **Индикаторы** для прямого перехода
- ✅ **Автоматическая смена** каждые 5 сек
- ✅ **Плавные анимации** (600ms)
- ✅ **Адаптивность** для всех экранов

---

## 💡 Дополнительные возможности:

### Свайп на мобильных (можно добавить):
```javascript
const handleTouchStart = (e) => {
  touchStartX = e.touches[0].clientX;
};

const handleTouchEnd = (e) => {
  const touchEndX = e.changedTouches[0].clientX;
  if (touchStartX - touchEndX > 50) nextCard();
  if (touchEndX - touchStartX > 50) prevCard();
};
```

### Клавиатурная навигация:
```javascript
useEffect(() => {
  const handleKeyPress = (e) => {
    if (e.key === 'ArrowLeft') prevCard();
    if (e.key === 'ArrowRight') nextCard();
  };
  window.addEventListener('keydown', handleKeyPress);
  return () => window.removeEventListener('keydown', handleKeyPress);
}, []);
```

---

## 🚀 Готово к использованию!

**Функционал:**
- [x] Кнопки навигации (влево/вправо) ✅
- [x] Плавная анимация смены ✅
- [x] Индикаторы карточек ✅
- [x] Автоматическая смена (5 сек) ✅
- [x] Адаптивность ✅
- [x] Неоморфный дизайн кнопок ✅
- [x] Золотые акценты индикаторов ✅
- [x] Нет linter ошибок ✅

**Carousel готов к работе!** 🎉✨

