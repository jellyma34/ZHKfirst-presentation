# Инструкции по настройке генплана благоустройства

## 🚀 Быстрый старт

### Шаг 1: Создайте папку для генплана

**Windows (PowerShell):**
```powershell
New-Item -Path "PUBLIC\plans\genplan" -ItemType Directory -Force
```

**Windows (CMD):**
```cmd
mkdir PUBLIC\plans\genplan
```

**Linux/Mac:**
```bash
mkdir -p PUBLIC/plans/genplan
```

---

### Шаг 2: Подготовьте изображения

#### Основное изображение генплана:

**Требования:**
- Формат: **JPG** или **PNG**
- Имя: `основное.jpg`
- Рекомендуемый размер: 1920x1080px или больше
- Содержание: Генеральный план территории с благоустройством

**Разместите:**
```
PUBLIC/plans/genplan/основное.jpg
```

#### Слои (PNG с прозрачностью):

**Требования:**
- Формат: **PNG** (обязательно с альфа-каналом)
- Размер: **идентичный** основному изображению
- Прозрачный фон везде, кроме нужных элементов
- Имена файлов (кириллица допустима):

```
озеленение.png           - Деревья, кустарники, газоны
детские-площадки.png     - Игровые зоны для детей
освещение.png            - Фонари, подсветка
парковки.png             - Парковочные места
пешеходные-зоны.png      - Тротуары, дорожки
зоны-отдыха.png          - Скамейки, беседки
```

**Разместите все в:**
```
PUBLIC/plans/genplan/
```

---

### Шаг 3: Скопируйте в dist (для production)

**Windows (PowerShell):**
```powershell
Copy-Item -Path "PUBLIC\plans\genplan" -Destination "dist\plans\" -Recurse -Force
```

**Windows (CMD):**
```cmd
xcopy /E /I /Y PUBLIC\plans\genplan dist\plans\genplan
```

**Linux/Mac:**
```bash
cp -r PUBLIC/plans/genplan dist/plans/
```

---

## 🎨 Как создать слои с прозрачностью

### Вариант 1: Photoshop

1. Откройте основное изображение генплана
2. Создайте новый слой
3. Выделите нужные зоны (например, озеленение)
4. Залейте цветом с 50-70% прозрачностью
5. Удалите фоновый слой (оставьте прозрачность)
6. **Сохраните как PNG** с альфа-каналом

### Вариант 2: GIMP (бесплатно)

1. Откройте изображение генплана
2. Добавьте альфа-канал: `Слой → Прозрачность → Добавить альфа-канал`
3. Создайте новый прозрачный слой
4. Нарисуйте/выделите нужные зоны
5. Экспортируйте как PNG

### Вариант 3: Figma/Canva

1. Создайте артборд размером как генплан
2. Установите прозрачный фон
3. Нарисуйте зоны нужным цветом
4. Экспортируйте как PNG

### Рекомендуемые цвета для слоёв:

```
Озеленение:         rgba(76, 175, 80, 0.6)   - Зеленый
Детские площадки:   rgba(255, 193, 7, 0.6)   - Желтый
Освещение:          rgba(255, 235, 59, 0.7)   - Светло-желтый
Парковки:           rgba(158, 158, 158, 0.6)  - Серый
Пешеходные зоны:    rgba(33, 150, 243, 0.5)   - Синий
Зоны отдыха:        rgba(233, 30, 99, 0.6)    - Розовый
```

---

## 🔄 Временное решение (если файлов еще нет)

### Используйте существующие изображения:

Код уже настроен на использование `/plans/general/main.jpg` как временного генплана.

**Чтобы протестировать без создания слоёв:**

Просто откройте страницу "Благоустройство" и прокрутите до секции "Генеральный план". Основное изображение будет отображаться, но слои не будут работать до создания PNG файлов.

---

## 📝 Структура файлов после настройки

```
PUBLIC/
└── plans/
    ├── general/
    │   └── main.jpg              ← Временный генплан
    │
    └── genplan/                  ← СОЗДАТЬ
        ├── основное.jpg          ← Основной генплан
        ├── озеленение.png        ← Слой 1
        ├── детские-площадки.png  ← Слой 2
        ├── освещение.png         ← Слой 3
        ├── парковки.png          ← Слой 4
        ├── пешеходные-зоны.png   ← Слой 5
        └── зоны-отдыха.png       ← Слой 6

dist/
└── plans/
    └── genplan/                  ← СКОПИРОВАТЬ
        └── (те же файлы)
```

---

## 🎯 Пошаговая инструкция

### 1. Создайте папку

```powershell
# Windows PowerShell
New-Item -Path "PUBLIC\plans\genplan" -ItemType Directory -Force
New-Item -Path "dist\plans\genplan" -ItemType Directory -Force
```

### 2. Добавьте основное изображение

Поместите файл генплана как:
```
PUBLIC/plans/genplan/основное.jpg
```

### 3. Обновите путь в коде (опционально)

Если хотите использовать основное изображение из genplan:

```jsx
// src/pages/Landscaping.jsx, строка ~259
- src="/plans/general/main.jpg"
+ src="/plans/genplan/основное.jpg"
```

### 4. Создайте и добавьте слои

Для каждого слоя:
1. Создайте PNG с прозрачностью
2. Назовите согласно списку
3. Поместите в `PUBLIC/plans/genplan/`

### 5. Скопируйте в dist

```powershell
Copy-Item -Path "PUBLIC\plans\genplan\*" -Destination "dist\plans\genplan\" -Recurse -Force
```

### 6. Протестируйте

1. Откройте страницу "Благоустройство"
2. Прокрутите до "Генеральный план"
3. Кликайте на фильтры слоёв
4. Проверьте наложение

---

## ⚠️ Важные замечания

### Размеры изображений:

Все PNG слои **ДОЛЖНЫ** иметь:
- Одинаковые размеры (width × height)
- Размеры совпадающие с основным изображением
- Прозрачный фон (альфа-канал)

### Формат файлов:

- **Основное:** JPG или PNG
- **Слои:** только PNG с прозрачностью

### Имена файлов:

- Можно использовать кириллицу
- Без пробелов в начале/конце
- Регистр имеет значение на Linux/Mac

### Консоль разработчика:

Открывайте консоль (F12) для отслеживания:
```
✅ Генплан загружен
✅ Слой загружен: озеленение
❌ Ошибка загрузки слоя: парковки  ← Проверьте файл
```

---

## 🎨 Готовые примеры

Если у вас пока нет изображений, можете:

1. **Использовать placeholder:**
   - Временно показывать `/plans/general/main.jpg`
   - Добавить текст "Генплан в разработке"

2. **Создать простые цветные зоны:**
   - Нарисуйте прямоугольники в Figma
   - Залейте нужными цветами
   - Экспортируйте как PNG

3. **Использовать существующие планы:**
   - Скопируйте из `/plans/section-1-floor-2/`
   - Переименуйте для демонстрации
   - Протестируйте функциональность

---

**После настройки у вас будет полноценный интерактивный генплан!** 🌳🏞️✨

