# 🎠 Слайдер с 3D-эффектом — Краткая инструкция

## ✅ Что реализовано

Главная страница обновлена: вместо одиночной карточки теперь **3D-слайдер** с центральной карточкой в фокусе и видимыми соседними элементами по бокам.

---

## 🎯 Основные возможности

### Визуализация
```
[Левая]    [ЦЕНТРАЛЬНАЯ]    [Правая]
  80%          100%            80%
opacity:     opacity:        opacity:
  0.7           1.0             0.7
```

### Управление
- **❮ / ❯** — ручная навигация
- **⏸ Остановить** — пауза автопрокрутки
- **▶ Воспроизвести** — возобновление автопрокрутки
- **Автопрокрутка** — каждые **6 секунд**

---

## 📂 Измененные файлы

1. **`src/pages/HomePage.jsx`** — компонент слайдера
2. **`src/styles/Home.css`** — стили слайдера (НОВЫЙ файл)

---

## 🎨 Ключевые особенности

### 1. **3D-эффект**
- Центральная карточка: `scale(1)`, `opacity: 1`
- Боковые карточки: `scale(0.8)`, `opacity: 0.7`, смещены на ±70%
- Скрытые карточки: `scale(0.5)`, `opacity: 0`

### 2. **Плавные анимации**
```css
transition: all 0.8s ease;
```

### 3. **Градиентный overlay**
Текст на карточке всегда читаем благодаря градиенту:
```css
background: linear-gradient(to top, rgba(0,0,0,0.9), rgba(0,0,0,0));
```

### 4. **Адаптивность**
- **Desktop**: видны 3 карточки (центр + 2 боковые)
- **Mobile**: видна только центральная карточка

---

## 📊 Данные слайдера

5 карточек с информацией:
1. **Информация об объекте капитального строительства**
2. **Обзорный план местоположения**
3. **Инженерно-геологические условия**
4. **Климатические условия**
5. **Эстетика и комфорт**

Изображения: `public/images/Фасадные решения-1.png` ... `5.png`

---

## 🔧 Технические детали

### Структура компонента
```jsx
const HomePage = () => {
  const slides = [...]; // 5 карточек
  const [index, setIndex] = useState(0);
  const [isPlaying, setIsPlaying] = useState(true);
  const intervalRef = useRef(null);

  // Автопрокрутка каждые 6 секунд
  useEffect(() => { ... }, [isPlaying, slides.length]);

  return (
    <div className="slider">
      <button onClick={prevSlide}>❮</button>
      <div className="slide-track">
        {/* 5 карточек с позиционированием */}
      </div>
      <button onClick={nextSlide}>❯</button>
    </div>
  );
};
```

### Логика позиционирования
```jsx
const offset = (i - index + slides.length) % slides.length;
let positionClass = "";
if (offset === 0) positionClass = "center";
else if (offset === 1 || ...) positionClass = "right";
else if (offset === slides.length - 1 || ...) positionClass = "left";
else positionClass = "hidden";
```

---

## 🚀 Запуск

```bash
npm run dev
```

Откройте браузер и перейдите на главную страницу.

---

## ✨ Результат

✅ **Центральная карточка** в фокусе, полного размера  
✅ **Боковые карточки** видны и уменьшены (80%)  
✅ **Плавные анимации** 0.8s ease  
✅ **Автопрокрутка** каждые 6 секунд  
✅ **Кнопка паузы/воспроизведения**  
✅ **Стрелки навигации** ❮ / ❯  
✅ **Адаптивность** для всех устройств  
✅ **Hover-эффекты** на кнопках  

---

**Дата**: 2025-10-07  
**Статус**: ✅ Готово к использованию

