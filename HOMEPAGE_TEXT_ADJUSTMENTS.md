# 📝 Корректировка текстов главной страницы

## ✅ Выполненные изменения

### 1. 🏷️ Заголовок страницы упрощен

**Было:**
```jsx
<h1 className="page-title">ЖК «Первый». 3 очередь строительства</h1>
```

**Стало:**
```jsx
<h1 className="page-title">3 очередь строительства</h1>
```

**Результат**: Убрано "ЖК «Первый»", оставлен только номер очереди

---

### 2. 📏 Текст описания карточек уменьшен в 1.5 раза

**Было:**
```css
.slide-info p {
  font-size: 0.95rem;
  line-height: 1.5;
  opacity: 0.95;
}
```

**Стало:**
```css
.slide-info p {
  font-size: 0.63rem; /* уменьшено в 1.5 раза */
  line-height: 1.4;
  white-space: pre-line;
  opacity: 0.9;
}
```

**Расчет**: `0.95 ÷ 1.5 ≈ 0.63 rem`

---

## 📊 Сравнение

| Элемент | До | После |
|---------|-----|--------|
| **Заголовок страницы** | ЖК «Первый». 3 очередь строительства | 3 очередь строительства |
| **Текст на карточках** | 0.95rem | 0.63rem **(÷1.5)** |
| **Line-height карточек** | 1.5 | 1.4 |
| **Opacity карточек** | 0.95 | 0.9 |

---

## 🎯 Что НЕ изменилось

✅ Позиционирование карточек  
✅ Стрелки переключения (-140px)  
✅ Размеры стрелок (65px)  
✅ Фон слайдера (прозрачный)  
✅ Размер текста описания под заголовком (1.8rem)  
✅ Все остальные элементы страницы  

---

## 📐 Визуализация изменений

### Заголовок страницы
```
До:  ЖК «Первый». 3 очередь строительства
       ↓ (убираем "ЖК «Первый»")
После: 3 очередь строительства
```

### Текст на карточках
```
До:  0.95rem (средний размер)
       ↓ (÷ 1.5)
После: 0.63rem (меньший размер)
```

---

## 📂 Измененные файлы

- ✏️ `src/pages/HomePage.jsx` — обновлен заголовок страницы
- ✏️ `src/styles/Home.css` — уменьшен размер текста на карточках

---

## 🎨 Детали стилей

### Заголовок h1
```css
.page-title {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 10px;
  color: #1a1a1a;
  font-family: 'Manrope', sans-serif;
}
```

### Текст на карточках
```css
.slide-info p {
  font-size: 0.63rem; /* уменьшено в 1.5 раза */
  line-height: 1.4;
  white-space: pre-line; /* поддержка переносов строк */
  opacity: 0.9;
}
```

---

## ✨ Преимущества

✅ **Заголовок короче** — быстрее воспринимается  
✅ **Текст карточек компактнее** — больше фокуса на изображениях  
✅ **Сохранена читаемость** — `line-height: 1.4` обеспечивает комфорт  
✅ **Все остальное без изменений** — стабильность дизайна  

---

## 🚀 Готово!

Запустите dev-сервер:

```bash
npm run dev
```

**Проверьте:**
1. Заголовок теперь "3 очередь строительства" (без "ЖК «Первый»")
2. Текст описания на карточках стал меньше и компактнее

---

**Дата**: 2025-10-07  
**Статус**: ✅ Изменения применены

