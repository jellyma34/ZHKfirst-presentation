# 🔍 Увеличение центрального блока с планом в 2 раза

## ✅ Выполненные изменения

### 1. 📏 Увеличение масштаба плана

**Файл**: `src/pages/PlanningSolutions.jsx`

**Было:**
```javascript
const [zoomLevel, setZoomLevel] = useState(65);
```

**Стало:**
```javascript
const [zoomLevel, setZoomLevel] = useState(130); // Увеличено в 2 раза
```

**Результат**: План теперь отображается с масштабом **130%** вместо **65%** = **увеличение в 2 раза**

---

### 2. 🔄 Обновление функции сброса

**Было:**
```javascript
const resetZoom = () => {
  setZoomLevel(65);
};
```

**Стало:**
```javascript
const resetZoom = () => {
  setZoomLevel(130); // Сброс к увеличенному масштабу
};
```

**Результат**: Кнопка "Сброс" теперь возвращает к **130%** (новому базовому масштабу)

---

### 3. 🎨 Добавлены CSS стили

**Файл**: `src/styles/planning-solutions.css`

#### Обертка для плана
```css
.plan-viewer-wrapper {
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 100%;
}
```

#### Контейнер плана
```css
.plan-container {
  transform-origin: center center; /* центрирование при масштабировании */
  transition: transform 0.3s ease-in-out;
}
```

#### Изображение плана
```css
.plan-image {
  margin: 0 auto; /* центрирование по горизонтали */
  transform-origin: center center; /* точка масштабирования */
  height: auto; /* автоматическая высота для сохранения пропорций */
}
```

---

## 📊 Сравнение параметров

| Параметр | До | После |
|----------|-----|--------|
| **Начальный масштаб** | 65% | **130%** (×2) |
| **Сброс масштаба** | 65% | **130%** |
| **Transform-origin** | не указан | **center center** |
| **Transition** | 0.3s ease | **0.3s ease-in-out** |

---

## 🎯 Как работает масштабирование

### Механизм
```javascript
// В компоненте PlanningSolutions.jsx (строка 295):
transform: `scale(${zoomLevel / 100})`

// Расчет:
zoomLevel = 130
scale = 130 / 100 = 1.3

// Сравнение:
Было: 65 / 100 = 0.65 (план был уменьшен до 65%)
Стало: 130 / 100 = 1.3 (план увеличен до 130%)

// Соотношение:
1.3 / 0.65 = 2 (увеличение в 2 раза)
```

---

## 📐 Визуализация

### До изменений
```
┌──────┐           ┌──────────┐           ┌──────┐
│      │           │          │           │      │
│ Выбор│           │   План   │           │Фильтры│
│ этажа│           │  (65%)   │           │ слоёв│
│      │           │          │           │      │
└──────┘           └──────────┘           └──────┘
```

### После изменений
```
┌──────┐       ┌──────────────────┐       ┌──────┐
│      │       │                  │       │      │
│ Выбор│       │      План        │       │Фильтры│
│ этажа│       │     (130%)       │       │ слоёв│
│      │       │   [в 2 раза]     │       │      │
└──────┘       └──────────────────┘       └──────┘
```

---

## 🎛️ Управление масштабом

### Диапазон зума
- **Минимум**: 25% (без изменений)
- **Максимум**: 200% (без изменений)
- **По умолчанию**: 130% (было 65%) ✅
- **Сброс**: 130% (было 65%) ✅

### Кнопки управления
- **➕ Zoom In**: увеличение на +25%
- **➖ Zoom Out**: уменьшение на -25%
- **🔄 Reset**: возврат к 130%
- **Slider**: ручная настройка 25-200%

---

## 📱 Адаптивность

### Desktop (> 1024px)
```javascript
zoomLevel: 130 // План в 2 раза больше
```

### Tablet (< 1024px)
```css
.plan-scaled {
  transform: scale(1) !important; /* На планшетах - нормальный размер */
}
```

### Mobile (< 768px)
```css
.floor-plan-container {
  height: 60vh; /* План остается крупным */
}
```

---

## ✨ Преимущества

✅ **План увеличен в 2 раза** — 65% → 130%  
✅ **Боковые панели без изменений** — остались прежнего размера  
✅ **Центрирование** — `transform-origin: center center`  
✅ **Плавные переходы** — `transition: 0.3s ease-in-out`  
✅ **Сброс работает** — возвращает к 130%  
✅ **Overflow: hidden** — план не выходит за границы  
✅ **Сохранены пропорции** — `height: auto`  

---

## 🔧 Технические детали

### Transform Scale
```css
/* Применяется inline в JSX: */
style={{
  transform: `scale(${zoomLevel / 100})`,
  transformOrigin: 'center top'
}}

/* С zoomLevel = 130: */
transform: scale(1.3)
```

### Transform-origin
```css
transform-origin: center center;
/* Масштабирование происходит от центра плана,
   а не от угла */
```

---

## 📂 Измененные файлы

- ✏️ `src/pages/PlanningSolutions.jsx` — `zoomLevel: 65 → 130`, `resetZoom: 65 → 130`
- ✏️ `src/styles/planning-solutions.css` — добавлены стили для увеличенного плана
- 📄 `PLAN_SCALE_INCREASE.md` — документация

---

## 🎯 Результат

**Центральный блок с планом увеличен в 2 раза:**
- Было: 65% (уменьшенный)
- Стало: 130% (увеличенный)
- Соотношение: ×2

**Боковые панели:**
- ✅ Без изменений
- ✅ "Выбор этажа" — прежний размер
- ✅ "Фильтры слоёв" — прежний размер

---

**Дата**: 2025-10-07  
**Статус**: ✅ Увеличено в 2 раза

